## API Report File for "@docx-editor.dev/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export const ACCEPTED_PARAGRAPH_PROPERTIES: readonly ["pStyle", "jc", "spacing", "ind", "tabs", "numPr", "keepNext", "keepLines", "widowControl", "pageBreakBefore", "shd"];

// @public
export const ACCEPTED_RUN_PROPERTIES: readonly ["rFonts", "sz", "szCs", "color", "b", "bCs", "i", "iCs", "u", "strike", "dstrike", "highlight", "vertAlign", "position", "caps", "smallCaps", "spacing", "w", "kern"];

// @public
export function addComment(store: TreeDocumentStore, request: AddCommentRequest): AddCommentResult;

// @public
export interface AddCommentRequest {
    // (undocumented)
    readonly anchor: CommentAnchorRequest;
    readonly author: string;
    readonly date?: string;
    // (undocumented)
    readonly initials?: string;
    readonly replyToCommentId?: string;
    // (undocumented)
    readonly text: string;
}

// @public
export type AddCommentResult = {
    readonly ok: true;
    readonly commentId: string;
    readonly change: TreeModelChange | null;
} | {
    readonly ok: false;
    readonly reason: TreeOpRejection | 'invalid-author';
};

// @public
export const ALL_FROZEN_IDS: readonly string[];

// @public
export function allocateContentControlId(root: OoxmlNode): number | null;

// @public
export function allocateDrawingPropertyId(pkg: OoxmlPackage): DrawingPropertyIdResult;

// @public
export function allocateNoteId(notesRoot: OoxmlNode): number | null;

// @public
export interface AnchorSnapshot {
    // (undocumented)
    readonly affinity: 'before' | 'after';
    // (undocumented)
    readonly anchorId: string;
    // (undocumented)
    readonly block: string;
    // (undocumented)
    readonly story: string;
}

// @public
export function applyEdits(part: OoxmlPart, edits: readonly ((current: OoxmlPart) => OoxmlEditResult)[], options?: EditOptions): OoxmlEditResult;

// @public
export function applyHeaderFooterLifecycleOp(pkg: OoxmlPackage, op: HeaderFooterLifecycleOp): HeaderFooterLifecycleResult;

// @public
export function applyNoteLifecycleOp(pkg: OoxmlPackage, op: NoteLifecycleOp, options?: NoteLifecycleOptions): NoteLifecycleResult;

// @public
export function applyTreeOp(part: OoxmlPart, op: TreeDocOp, options?: EditOptions): TreeOpResult;

// @public
export function asciiFold(s: string): string;

// @public
export function assertLimitInvariants(): void;

// @public
export function assertValidId(id: string, kind?: IdKind): void;

// @public
export function assertValidQName(name: string): void;

// @public
export interface AtomicFieldSpan {
    readonly formatRunIds: readonly string[];
    // (undocumented)
    readonly kind: 'complex' | 'simple';
    readonly node: OoxmlNode;
    // (undocumented)
    readonly removeNodeIds: readonly string[];
    readonly runId: string;
}

// @public
export function atomicFieldSpansOf(paragraph: OoxmlParagraphNode, options?: {
    readonly maxNesting?: number;
    readonly maxInstructionChars?: number;
}): readonly AtomicFieldSpan[];

// @public
export interface AtomicNoteSpan {
    // (undocumented)
    readonly kind: 'noteReference' | 'noteRef' | 'separator' | 'continuationSeparator';
    // (undocumented)
    readonly node: OoxmlNode;
    // (undocumented)
    readonly removeNodeIds: readonly string[];
    // (undocumented)
    readonly runId: string;
}

// @public
export function atomicNoteSpansOf(paragraph: OoxmlParagraphNode): readonly AtomicNoteSpan[];

// @public
export interface AuditPort {
    // (undocumented)
    record(entry: {
        readonly kind: string;
        readonly at: number;
        readonly meta?: Record<string, unknown>;
    }): void;
}

// @public
export const AUTHORABLE_PARAGRAPH_PROPERTIES: ReadonlySet<string>;

// @public
export const AUTHORABLE_RUN_PROPERTIES: ReadonlySet<string>;

// @public
export function authorableHyperlinkTarget(url: string): string | null;

// @public
export function authoredDocumentEndnoteProperties(settings: OoxmlPart | null | undefined): AuthoredNoteProperties | undefined;

// @public
export function authoredDocumentFootnoteProperties(settings: OoxmlPart | null | undefined): AuthoredNoteProperties | undefined;

// @public
export function authoredEndnotePropertiesFromSectPr(sectPr: OoxmlNode | null | undefined): AuthoredNoteProperties | undefined;

// @public
export function authoredFootnotePropertiesFromSectPr(sectPr: OoxmlNode | null | undefined): AuthoredNoteProperties | undefined;

// @public
export interface AuthoredNoteProperties {
    // (undocumented)
    readonly numFmt?: string;
    // (undocumented)
    readonly numRestart?: string;
    // (undocumented)
    readonly numStart?: number;
    // (undocumented)
    readonly pos?: string;
}

// @public
export function authoredProperties(container: OoxmlNode | undefined, authorable: ReadonlySet<string>): readonly OoxmlProperty[];

// @public
export interface AuthorizationPort {
    // (undocumented)
    authorize(action: string, subject?: string): boolean;
}

// @public
export function beginOperation(init: OperationInit): OperationContext;

// @public
export function bodyStoryRoot(part: OoxmlPart): OoxmlNode | null;

// @public
export interface BookmarkAnchor {
    // (undocumented)
    readonly name: string;
    readonly offset: number;
    readonly paragraphId: string;
}

// @public
export type BookmarkIndex = ReadonlyMap<string, BookmarkAnchor>;

// @public
export function bookmarkPairNodes(mint: () => string, name: string, id: string): {
    readonly start: OoxmlNode;
    readonly end: OoxmlNode;
};

// @public
export function boundCustomXmlNodeIdOf(control: OoxmlNode, storeItemId: string): string | null;

// @public
export function boundCustomXmlNodeIds(part: OoxmlPart, storeItemId: string): Set<string>;

// @public
export function boundCustomXmlNodeIdsInPackage(pkg: OoxmlPackage, storeItemId: string): Set<string>;

// @public
export class BoundedCounter {
    constructor(label: string, limit: number);
    add(n?: number): number;
    canAdd(n?: number): boolean;
    // (undocumented)
    get current(): number;
    // (undocumented)
    readonly label: string;
    // (undocumented)
    readonly limit: number;
    // (undocumented)
    release(n: number): void;
    get remaining(): number;
}

// @public
export class Budget {
    constructor(label: string, capacity: number);
    // (undocumented)
    get available(): number;
    // (undocumented)
    readonly capacity: number;
    child(label: string, capacity: number): Budget;
    dispose(): void;
    // (undocumented)
    get inUse(): number;
    // (undocumented)
    get isDisposed(): boolean;
    // (undocumented)
    readonly label: string;
    onRelease(cleanup: () => void): void;
    reserve(amount: number): Reservation;
}

// @public
export class BudgetError extends Error {
    constructor(message: string);
}

// @public
export function buildBookmarkIndex(part: OoxmlPart): BookmarkIndex;

// @public
export function buildContentTypeIndex(records: ContentTypeRecords, maxRecords?: number): IndexResult;

// @public
export function buildRelationshipSet(records: readonly RelationshipRecord[]): RelationshipSetResult;

// @public
export function buildTocContentControl(mint: () => string, entries: readonly TocEntryPlan[], instruction: TocInstruction, alias: string): OoxmlNode;

// @public
export function buildTocEntryParagraph(mint: () => string, entry: TocEntryPlan, instruction: TocInstruction, paragraphPropertiesTemplate?: OoxmlNode): OoxmlNode;

// @public
export class CancellationController {
    // (undocumented)
    cancel(reason?: string): void;
    get derivedOnly(): boolean;
    // (undocumented)
    get isCancelled(): boolean;
    // (undocumented)
    get isPublished(): boolean;
    markPublished(): void;
    // (undocumented)
    get phase(): CancellationPhase;
    // (undocumented)
    get token(): CancellationToken;
}

// @public
export class CancellationError extends Error {
    constructor(
    derivedOnly: boolean, reason?: string);
    readonly derivedOnly: boolean;
}

// @public
export type CancellationPhase = 'pre-publication' | 'post-publication';

// @public
export interface CancellationPort {
    // (undocumented)
    readonly token: CancellationToken;
}

// @public
export interface CancellationToken {
    checkpoint(): void;
    readonly derivedOnly: boolean;
    // (undocumented)
    readonly isCancelled: boolean;
    // (undocumented)
    readonly phase: CancellationPhase;
}

// @public
export const CANONICAL_MUTATION_ORIGINS: readonly string[];

// @public
export function canonicalize(value: unknown, ephemera?: ReadonlySet<string>): string;

// @public
export function canonicalOoxmlFingerprint(value: OoxmlPart | OoxmlNode): string;

// @public
export interface CapabilityId {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly kind: IdKind;
    // (undocumented)
    readonly version: string;
}

// @public
export function cascadeDeletedNoteReferences(before: OoxmlPackage, after: OoxmlPackage, options?: CascadeDeletedNoteReferencesOptions): OoxmlPackage | null;

// @public
export interface CascadeDeletedNoteReferencesOptions {
    // (undocumented)
    readonly afterBudget?: NoteReferenceScanBudget;
    readonly beforeBudget?: NoteReferenceScanBudget;
}

// @public
export function cascadeEmptiedComments(before: OoxmlPackage, after: OoxmlPackage, owner?: CommentDeletionOwner): OoxmlPackage | null;

// @public
export function childElements(node: Extract<XmlNode, {
    type: 'element';
}>, name: string): Extract<XmlNode, {
    type: 'element';
}>[];

// @public
export interface ClockPort {
    // (undocumented)
    now(): number;
}

// @public
export function collectFlowBlocks(children: readonly OoxmlNode[], depth?: number, accept?: (block: OoxmlElement) => boolean): OoxmlElement[];

// @public
export function collectNodeIds(part: OoxmlPart): Set<string>;

// @public
export function collectNoteReferences(part: OoxmlPart, options?: {
    readonly maxHits?: number;
    readonly budget?: NoteReferenceScanBudget;
}): readonly NoteReferenceHit[];

// @public
export function collectPackageNoteReferences(pkg: OoxmlPackage, options?: {
    readonly budget?: NoteReferenceScanBudget;
    readonly maxHits?: number;
}): readonly NoteReferenceHit[];

// @public
export function collectReviewItems(input: ReviewModelInput): ReviewItem[];

// @public
export function collectRevisionSites(part: OoxmlPart): RevisionSite[];

// @public
export function collectSectionPropertyNodes(root: OoxmlNode): Array<OoxmlElement | null>;

// @public
export function collectStoryParagraphs(children: readonly OoxmlNode[], out: OoxmlNode[], sdtDepth: number): void;

// @public
export interface CommentAnchor {
    // (undocumented)
    readonly commentId: string;
    // (undocumented)
    readonly end: CommentPosition;
    // (undocumented)
    readonly orphaned: boolean;
    readonly partName: string;
    // (undocumented)
    readonly start: CommentPosition;
}

// @public
export interface CommentAnchorRequest {
    readonly end: number;
    // (undocumented)
    readonly endParagraphId?: string;
    // (undocumented)
    readonly paragraphId: string;
    // (undocumented)
    readonly start: number;
}

// @public
export function commentAnchorsOfStory(part: OoxmlPart): CommentAnchor[];

// @public
export function commentBodyText(comment: CommentRecord): string;

// @public
export interface CommentDeletionOwner {
    readonly noteId?: number;
    readonly storyPartName: string;
}

// @public
export function commentInitials(comment: CommentRecord): string;

// @public
export function commentItemsOf(comments: readonly CommentRecord[], anchors: readonly {
    commentId: string;
    partName: string;
    start: ReviewPosition;
    end: ReviewPosition;
    orphaned: boolean;
}[], threadState: ReadonlyMap<string, CommentThreadState>): ReviewCommentItem[];

// @public
export function commentPartNameOf(pkg: OoxmlPackage, storyPartName: string): string;

// @public
export interface CommentPosition {
    // (undocumented)
    readonly offset: number;
    // (undocumented)
    readonly paragraphId: string;
}

// @public
export interface CommentRecord {
    // (undocumented)
    readonly author: string;
    readonly blocks: readonly OoxmlElement[];
    // (undocumented)
    readonly date?: string;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly initials?: string;
    readonly paraId?: string;
    readonly parentCommentId?: string;
}

// @public
export function commentsExtendedPartNameOf(pkg: OoxmlPackage, storyPartName: string): string;

// @public
export function commentsOfPart(part: OoxmlPart): CommentRecord[];

// @public
export interface CommentThreadState {
    // (undocumented)
    readonly done: boolean;
    readonly parentParaId?: string;
}

// @public
export interface ComparatorDescriptor {
    readonly ephemera: readonly string[];
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly mode: ComparatorMode;
    // (undocumented)
    readonly note: string;
}

// @public
export type ComparatorMode = 'canonical-exact' | 'exact' | 'tolerance' | 'sync-optimization-only';

// @public
export type ComparatorName = keyof typeof COMPARATORS;

// @public
export const COMPARATORS: {
    readonly authoredState: {
        readonly id: "dev.docx-editor.core.comparator.authored-state";
        readonly mode: "canonical-exact";
        readonly ephemera: readonly ["revision", "provenance", "producedAt", "commitId"];
        readonly note: "canonical normalized authored records; ephemera excluded";
    };
    readonly anchor: {
        readonly id: "dev.docx-editor.core.comparator.anchor";
        readonly mode: "exact";
        readonly ephemera: readonly [];
        readonly note: "internal anchor identity/affinity compares exactly";
    };
    readonly yjsStateVector: {
        readonly id: "dev.docx-editor.core.comparator.yjs-state-vector";
        readonly mode: "sync-optimization-only";
        readonly ephemera: readonly [];
        readonly note: "exchange optimization only; never proves update or delete-set coverage";
    };
    readonly shapedRun: {
        readonly id: "dev.docx-editor.core.comparator.shaped-run";
        readonly mode: "exact";
        readonly ephemera: readonly [];
        readonly note: "glyph ids, clusters, and fixed-point advances compare exactly";
    };
    readonly paginationFingerprint: {
        readonly id: "dev.docx-editor.core.comparator.pagination-fingerprint";
        readonly mode: "exact";
        readonly ephemera: readonly [];
        readonly note: "page/column boundaries, break causes, fixed-point geometry compare exactly";
    };
    readonly semanticTree: {
        readonly id: "dev.docx-editor.core.comparator.semantic-tree";
        readonly mode: "exact";
        readonly ephemera: readonly [];
        readonly note: "reading order, roles, headings, alt text compare exactly";
    };
    readonly hitTest: {
        readonly id: "dev.docx-editor.core.comparator.hit-test";
        readonly mode: "exact";
        readonly ephemera: readonly [];
        readonly note: "resolved hit target and cluster affinity compare exactly";
    };
    readonly pdfSemantics: {
        readonly id: "dev.docx-editor.core.comparator.pdf-semantics";
        readonly mode: "canonical-exact";
        readonly ephemera: readonly ["objectNumber", "producer", "creationDate", "modDate", "subsetTag"];
        readonly note: "canonical semantic PDF objects; container ephemera excluded";
    };
    readonly rasterCheckpoint: {
        readonly id: "dev.docx-editor.core.comparator.raster-checkpoint";
        readonly mode: "tolerance";
        readonly ephemera: readonly [];
        readonly note: "documented unavoidable raster comparison; explicit tolerance only";
    };
    readonly benchmarkEvidence: {
        readonly id: "dev.docx-editor.core.comparator.benchmark-evidence";
        readonly mode: "sync-optimization-only";
        readonly ephemera: readonly [];
        readonly note: "diagnostic evidence, not an equivalence basis";
    };
};

// @public
export function compareArtifacts(name: ComparatorName, left: unknown, right: unknown, opts?: {
    epsilon?: number;
}): ComparisonResult;

// @public
export function compareSemVer(a: SemVer, b: SemVer): -1 | 0 | 1;

// @public
export interface ComparisonResult {
    // (undocumented)
    readonly equal: boolean;
    readonly left?: string;
    // (undocumented)
    readonly right?: string;
}

// @public
export interface ConformanceFixture {
    // (undocumented)
    readonly documentId: string;
    // (undocumented)
    readonly formatVersion: 1;
    // (undocumented)
    readonly snapshots?: readonly EncodedEnvelope[];
    // (undocumented)
    readonly source: FixtureSource;
    // (undocumented)
    readonly steps: readonly FixtureStep[];
    // (undocumented)
    readonly updates?: readonly EncodedEnvelope[];
}

// @public
export function containsCssFetch(value: string): boolean;

// @public
export const CONTENT_CONTROL_ID_MAX = 2147483647;

// @public
export const CONTENT_CONTROL_PROPERTY_ORDER: readonly string[];

// @public
export interface ContentControlCheckbox {
    // (undocumented)
    readonly checked: boolean;
    // (undocumented)
    readonly checkedState?: ContentControlCheckboxState;
    // (undocumented)
    readonly uncheckedState?: ContentControlCheckboxState;
}

// @public
export interface ContentControlCheckboxState {
    // (undocumented)
    readonly font?: string;
    readonly value: string;
}

// @public
export function contentControlContentChildren(wrapper: OoxmlNode): readonly OoxmlNode[];

// @public
export function contentControlContentNodeOf(control: OoxmlNode): OoxmlContentControlContentNode | undefined;

// @public
export function contentControlContentOf(node: OoxmlNode): readonly OoxmlNode[] | null;

// @public
export interface ContentControlDataBinding {
    // (undocumented)
    readonly prefixMappings?: string;
    // (undocumented)
    readonly storeItemID?: string;
    // (undocumented)
    readonly xpath?: string;
}

// @public
export interface ContentControlDateFormat {
    // (undocumented)
    readonly calendar?: string;
    // (undocumented)
    readonly dateFormat?: string;
    // (undocumented)
    readonly fullDate?: string;
    // (undocumented)
    readonly lid?: string;
    // (undocumented)
    readonly storeMappedDataAs?: string;
}

// @public
export function contentControlEndPropertiesNodeOf(control: OoxmlNode): OoxmlContentControlEndPropertiesNode | undefined;

// @public
export interface ContentControlEntry {
    readonly ancestors: readonly OoxmlContentControlNode[];
    readonly depth: number;
    // (undocumented)
    readonly node: OoxmlContentControlNode;
}

// @public
export type ContentControlKind = 'richText' | 'plainText' | 'checkbox' | 'dropDownList' | 'comboBox' | 'date' | 'picture' | 'docPartObj' | 'docPartList' | 'group' | 'citation' | 'bibliography' | 'equation' | 'untyped';

// @public
export type ContentControlLevel = 'block' | 'inline' | 'row' | 'cell' | 'empty';

// @public
export function contentControlLevelOf(control: OoxmlNode): ContentControlLevel;

// @public
export interface ContentControlListItem {
    // (undocumented)
    readonly displayText: string;
    // (undocumented)
    readonly value: string;
}

// @public
export type ContentControlLock = 'unlocked' | 'sdtLocked' | 'contentLocked' | 'sdtContentLocked';

// @public
export interface ContentControlProperties {
    // (undocumented)
    readonly alias?: string;
    // (undocumented)
    readonly checkbox?: ContentControlCheckbox;
    // (undocumented)
    readonly dataBinding?: ContentControlDataBinding;
    // (undocumented)
    readonly date?: ContentControlDateFormat;
    readonly id?: number;
    // (undocumented)
    readonly label?: string;
    // (undocumented)
    readonly lastValue?: string;
    readonly listItems: readonly ContentControlListItem[];
    // (undocumented)
    readonly lock: ContentControlLock;
    // (undocumented)
    readonly multiLine?: boolean;
    readonly placeholderDocPart?: string;
    // (undocumented)
    readonly showingPlaceholder: boolean;
    // (undocumented)
    readonly tabIndex?: number;
    // (undocumented)
    readonly tag?: string;
    // (undocumented)
    readonly temporary: boolean;
    // (undocumented)
    readonly type: ContentControlKind;
}

// @public
export function contentControlPropertiesNodeOf(control: OoxmlNode): OoxmlContentControlPropertiesNode | undefined;

// @public
export function contentControlPropertiesOf(control: OoxmlNode): ContentControlProperties;

// @public
export function contentControlsIn(root: OoxmlNode, options?: {
    readonly maxDepth?: number;
    readonly limit?: number;
}): readonly ContentControlEntry[];

// @public
export function contentControlTextOf(control: OoxmlNode): string;

// @public
export interface ContentItem {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly kind: string;
}

// @public
export type ContentTypeError = {
    readonly code: 'invalid-mime';
    readonly value: string;
} | {
    readonly code: 'conflicting-default';
    readonly extension: string;
} | {
    readonly code: 'duplicate-override';
    readonly partName: string;
} | {
    readonly code: 'invalid-override-name';
    readonly partName: string;
} | {
    readonly code: 'too-many-records';
    readonly limit: number;
};

// @public
export interface ContentTypeIndex {
    readonly defaults: ReadonlyMap<string, string>;
    readonly overrides: ReadonlyMap<string, string>;
}

// @public
export interface ContentTypeRecords {
    // (undocumented)
    readonly defaults: readonly DefaultRecord[];
    // (undocumented)
    readonly overrides: readonly OverrideRecord[];
}

// @public
export function contentTypesPartBytes(pkg: OoxmlPackage): {
    readonly storageKey: string;
    readonly bytes: Uint8Array;
} | null;

// @public
export interface Contribution {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly kind: Exclude<IdKind, 'extension' | 'origin' | 'result'>;
    // (undocumented)
    readonly payload?: unknown;
    readonly replaceable?: ReplacementPolicy;
    readonly replaces?: {
        readonly targetId: string;
        readonly targetRange: string;
        readonly priority?: number;
    };
    // (undocumented)
    readonly version: string;
}

// @public
export function createImageResourceCache(initialPkg: OoxmlPackage, options: CreateImageResourceCacheOptions): ImageResourceLookup;

// @public
export interface CreateImageResourceCacheOptions {
    // (undocumented)
    readonly decodePort: ImageDecodePort;
    // (undocumented)
    readonly limits?: Partial<ImageResourceLimits>;
}

// @public
export function createNodeIdAllocator(part: OoxmlPart): () => string;

// @public
export function createNoteReferenceScanBudget(maxVisited?: number, maxParts?: number): NoteReferenceScanBudget;

// @public
export const CUSTOM_NODE_XPATH_PREFIX = "ns0";

// @public
export const CUSTOM_XML_PROPS_REL = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/customXmlProps";

// @public
export const CUSTOM_XML_PROPS_TYPE = "application/vnd.openxmlformats-officedocument.customXmlProperties+xml";

// @public
export const CUSTOM_XML_REL = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/customXml";

// @public
export function customMarkFollows(node: OoxmlNode): boolean | undefined;

// @public
export interface CustomNodeBinding {
    // (undocumented)
    readonly prefixMappings: string;
    // (undocumented)
    readonly storeItemId: string;
    // (undocumented)
    readonly xpath: string;
}

// @public
export function customNodeBinding(part: CustomXmlDataPart, rootLocalName: string, nodeId: string): CustomNodeBinding | null;

// @public
export type CustomNodeExportPolicy = 'keep' | 'text' | 'remove';

// @public
export interface CustomNodeExportRequest {
    readonly decide: (tag: string) => CustomNodeExportPolicy;
    readonly namespaces: readonly string[];
    readonly storyPartName: string;
}

// @public
export type CustomNodeExportResult = {
    readonly ok: true;
    readonly pkg: OoxmlPackage;
    readonly unwrapped: number;
    readonly removed: number;
} | {
    readonly ok: false;
    readonly reason: string;
};

// @public
export interface CustomNodePayloadRead {
    readonly data: string;
    readonly label: string;
    readonly nodeId: string;
}

// @public
export function customNodePayloadsByControl(pkg: OoxmlPackage, storyPartName: string): ReadonlyMap<string, CustomNodePayloadRead>;

// @public
export function customNodePayloadsOf(pkg: OoxmlPackage, storyPartName: string, namespaceUri: string): ReadonlyMap<string, {
    readonly label: string;
    readonly data: string;
}>;

// @public
export interface CustomNodePayloadWrite {
    readonly data: string;
    readonly label: string;
    readonly namespaceUri: string;
    readonly nodeId: string;
    readonly rootLocalName: string;
}

// @public
export type CustomNodeSweepOutcome = {
    readonly ok: true;
    readonly removed: readonly string[];
} | {
    readonly ok: false;
    readonly reason: string;
};

// @public
export type CustomNodeSweepResult = {
    readonly ok: true;
    readonly pkg: OoxmlPackage;
    readonly removed: readonly string[];
} | {
    readonly ok: false;
    readonly reason: string;
};

// @public
export type CustomNodeWriteRejection = TreeOpRejection
/** The id, root name or namespace cannot be spelled in an XPath, so no binding could name it. */
| 'unaddressable-payload'
/** The store could not be authored — see `withCustomXmlDataPart` for every way that happens. */
| 'store-not-authored'
/** The payload or the label is past the cap. */
| 'payload-too-large';

// @public (undocumented)
export type CustomNodeWriteResult = {
    readonly ok: true;
    readonly change: TreeModelChange | null;
    readonly nodeId?: string;
} | {
    readonly ok: false;
    readonly reason: CustomNodeWriteRejection;
    readonly detail?: string;
};

// @public
export interface CustomXmlDataPart {
    readonly itemId: string;
    readonly namespaceUri: string;
    readonly partName: string;
    readonly propsPartName: string;
}

// @public
export interface CustomXmlDataPartResult {
    readonly part: CustomXmlDataPart | null;
    // (undocumented)
    readonly pkg: OoxmlPackage;
}

// @public
export function customXmlDataParts(pkg: OoxmlPackage, storyPartName: string): CustomXmlDataPart[];

// @public
export function customXmlLabelXPath(prefix: string, rootLocalName: string, nodeId: string): string | null;

// @public
export interface CustomXmlNode {
    readonly data: string;
    // (undocumented)
    readonly id: string;
    readonly label: string;
}

// @public
export function customXmlNodes(pkg: OoxmlPackage, partName: string): CustomXmlNode[];

// @public
export function customXmlPrefixMappings(prefix: string, namespaceUri: string): string | null;

// @public
export const DANGEROUS_KEYS: readonly string[];

// @public
export class DangerousKeyError extends Error {
    constructor(key: string, path: string);
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly path: string;
}

// @public
export const DATASTORE_NAMESPACE_URI = "http://schemas.openxmlformats.org/officeDocument/2006/customXml";

// @public
export function datastoreItemIdFor(seed: string): string;

// @public
export function deepParagraphOrderOfPart(part: OoxmlPart): ReadonlyMap<string, number>;

// @public
export const DEFAULT_ENDNOTE_PROPERTIES: ResolvedEndnoteProperties;

// @public
export const DEFAULT_FOOTNOTE_PROPERTIES: ResolvedFootnoteProperties;

// @public
export const DEFAULT_IMAGE_RESOURCE_LIMITS: ImageResourceLimits;

// @public
export const DEFAULT_LIMITS: ResourceLimits;

// @public
export const DEFAULT_MAX_EDITABLE_STORY_PARTS = 64;

// @public
export const DEFAULT_OOXML_PACKAGE_LIMITS: Required<Pick<OoxmlPackageLimits, 'maxXmlParts' | 'maxRelationships'>>;

// @public
export const DEFAULT_VIEW_SETTINGS: DocumentViewSettings;

// @public
export const DEFAULT_ZIP_LIMITS: ZipLimits;

// @public
export interface DefaultRecord {
    // (undocumented)
    readonly contentType: string;
    // (undocumented)
    readonly extension: string;
    // (undocumented)
    readonly order: number;
}

// @public
export function deleteCommentThread(pkg: OoxmlPackage, commentId: string, owner?: CommentDeletionOwner): OoxmlPackage | null;

// @public
export function deobfuscateFont(bytes: Uint8Array, fontKey: string): Uint8Array | null;

// @public
export const DEPENDENCY_KEY_IDS: {
    readonly style: "dev.docx-editor.core.dep.style";
    readonly numbering: "dev.docx-editor.core.dep.numbering";
    readonly section: "dev.docx-editor.core.dep.section";
    readonly story: "dev.docx-editor.core.dep.story";
    readonly font: "dev.docx-editor.core.dep.font";
    readonly image: "dev.docx-editor.core.dep.image";
    readonly table: "dev.docx-editor.core.dep.table";
    readonly field: "dev.docx-editor.core.dep.field";
    readonly note: "dev.docx-editor.core.dep.note";
    readonly headerFooter: "dev.docx-editor.core.dep.header-footer";
    readonly annotation: "dev.docx-editor.core.dep.annotation";
};

// @public
export function deriveOoxmlIndexes(pkg: OoxmlPackage, revision: number): OoxmlIndexes;

// @public
export function detectBodyTocs(part: OoxmlPart): readonly DetectedToc[];

// @public
export function detectDuplicateNames(rawNames: readonly string[]): {
    readonly duplicates: readonly string[];
    readonly rejected: readonly {
        raw: string;
        reason: NameRejection;
    }[];
};

// @public
export interface DetectedToc {
    // (undocumented)
    readonly beginNodeId: string;
    // (undocumented)
    readonly beginParagraphId: string;
    readonly containerId: string;
    // (undocumented)
    readonly contentControlId?: string;
    // (undocumented)
    readonly endParagraphId: string;
    readonly id: string;
    // (undocumented)
    readonly instruction: TocInstruction;
    // (undocumented)
    readonly resultParagraphIds: readonly string[];
}

// @public
export class DeterministicClock implements ClockPort {
    constructor(start?: number, step?: number);
    // (undocumented)
    now(): number;
}

// @public
export class DeterministicMemoryMeter {
    constructor(limitBytes: number);
    // (undocumented)
    allocate(bytes: number): void;
    // (undocumented)
    get currentBytes(): number;
    // (undocumented)
    free(bytes: number): void;
    // (undocumented)
    get peakBytes(): number;
    reset(): void;
}

// @public
export function diagnoseNoteReferences(pkg: OoxmlPackage): readonly NoteDiagnostic[];

// @public
export function diffSemanticDigests(before: SemanticDigest, after: SemanticDigest): DigestDifference[];

// @public
export type DigestDifference = {
    readonly path: string;
    readonly before: string;
    readonly after: string;
};

// @public
export function digestPart(part: OoxmlPart): StoryDigest | null;

// @public
export function directParagraphMarkProperties(part: OoxmlPart, paragraphId: string): readonly OoxmlProperty[];

// @public
export function directParagraphProperties(part: OoxmlPart, paragraphId: string): readonly OoxmlProperty[];

// @public
export interface DocumentProperties {
    readonly company?: string;
    readonly creator?: string;
    readonly description?: string;
    readonly keywords?: string;
    readonly lastModifiedBy?: string;
    readonly manager?: string;
    readonly subject?: string;
    readonly title?: string;
}

// @public
export interface DocumentTrackingSettings {
    readonly doNotTrackFormatting: boolean;
    readonly doNotTrackMoves: boolean;
    readonly restrictedToTrackedChanges: boolean;
    readonly trackRevisions: boolean;
}

// @public
export interface DocumentViewSettings {
    readonly doNotShadeFormData: boolean;
}

// @public
export type DrawingKind = 'inline' | 'anchored';

// @public
export interface DrawingLocks {
    // (undocumented)
    readonly changeAspect: boolean;
    // (undocumented)
    readonly move: boolean;
    // (undocumented)
    readonly resize: boolean;
    // (undocumented)
    readonly select: boolean;
}

// @public
export function drawingOpImpact(op: DrawingTreeDocOp): ImpactClass;

// @public
export interface DrawingPositionInput {
    // (undocumented)
    readonly horizontalEmu?: number;
    readonly mode?: 'frame' | 'simple';
    // (undocumented)
    readonly relativeToH?: DrawingHorizontalReferenceFrame;
    // (undocumented)
    readonly relativeToV?: DrawingVerticalReferenceFrame;
    // (undocumented)
    readonly verticalEmu?: number;
}

// @public
export type DrawingPropertyIdResult = {
    readonly ok: true;
    readonly id: number;
} | {
    readonly ok: false;
    readonly reason: 'invalidArgs';
};

// @public
export type DrawingTreeDocOp = Extract<TreeDocOp, {
    readonly op: 'insertDrawing' | 'replaceDrawingResource' | 'deleteDrawing' | 'resizeDrawing' | 'cropDrawing' | 'positionDrawing' | 'setDrawingWrap' | 'setDrawingMetadata' | 'setDrawingLocks' | 'transformDrawing';
}>;

// @public
export interface EditOptions {
    // (undocumented)
    readonly deferValidation?: boolean;
}

// @public
export interface EmbeddedFont {
    readonly bytes: Uint8Array;
    readonly family: string;
    readonly partName: string;
    // (undocumented)
    readonly style: FontStyleKey;
}

// @public
export interface EncodedEnvelope {
    // (undocumented)
    readonly byteLength: number;
    readonly bytesHex: string;
    // (undocumented)
    readonly documentId: string;
    // (undocumented)
    readonly kind: 'snapshot' | 'update';
    // (undocumented)
    readonly protocolVersion: number;
    // (undocumented)
    readonly schemaVersion: number;
}

// @public
export type EndnotePosition = 'sectEnd' | 'docEnd';

// @public
export function endOperation(ctx: OperationContext): void;

// @public
export type EnforcementPhase = 'package-read' | 'xml-parse' | 'layout' | 'output';

// @public
export interface EnsuredHyperlinkRelationship {
    // (undocumented)
    readonly pkg: OoxmlPackage;
    // (undocumented)
    readonly relationshipId: string;
}

// @public
export function ensureHyperlinkRelationship(pkg: OoxmlPackage, url: string, ownerPart?: string): EnsuredHyperlinkRelationship | null;

// @public
export function ensureListDefinition(pkg: OoxmlPackage, kind: ListKind): EnsuredListDefinition | null;

// @public
export function ensureNumberingLevel(pkg: OoxmlPackage, numId: string, level: number, kind: ListKind): OoxmlPackage | null;

// @public
export function escapeCssString(value: string): string;

// @public
export function escapeXml(value: string): string;

// @public
export function extensionKey(extension: string): string;

// @public
export interface ExternalResourceConsentPort {
    // (undocumented)
    requestConsent(target: string): boolean;
}

// @public
export interface FeatureBundle {
    // (undocumented)
    readonly conflicts?: readonly string[];
    // (undocumented)
    readonly contributions: readonly Contribution[];
    // (undocumented)
    readonly dependencies?: readonly string[];
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly requiredPorts?: readonly string[];
    // (undocumented)
    readonly version: string;
}

// @public
export const FIELD_ATOM_CHAR = "\uFFFC";

// @public
export function fieldAtomText(): typeof FIELD_ATOM_CHAR;

// @public
export function fieldOnOffAttribute(node: OoxmlNode, localName: 'dirty' | 'fldLock'): boolean | undefined;

// @public
export function findContentControl(root: OoxmlNode, nodeId: string): ContentControlEntry | null;

// @public
export function findCustomXmlDataPart(pkg: OoxmlPackage, storyPartName: string, namespaceUri: string): CustomXmlDataPart | null;

// @public
export function findDetectedToc(tocs: readonly DetectedToc[], tocId: string): DetectedToc | null;

// @public
export function findElement(nodes: readonly XmlNode[], name: string): Extract<XmlNode, {
    type: 'element';
}> | undefined;

// @public
export function findNode(part: OoxmlPart, nodeId: string): OoxmlNode | null;

// @public
export function findNoteById(root: OoxmlNode, noteId: number): OoxmlNoteNode | undefined;

// @public
export function findOccurrences(text: string, query: string, limit: number, options?: TextMatchOptions): TextOccurrences;

// @public
export function fingerprint(name: ComparatorName, value: unknown): string;

// @public
export function firstReviewRange(item: ReviewItem): ReviewRange | null;

// @public
export interface FixtureExpectation {
    // (undocumented)
    readonly anchors?: readonly AnchorSnapshot[];
    readonly authoredStateHash?: string;
    readonly committedRevision?: number;
    // (undocumented)
    readonly modelChange?: ModelChangeSummary;
    // (undocumented)
    readonly outcome: FixtureOutcome;
    // (undocumented)
    readonly outputHash?: string;
}

// @public
export type FixtureOutcome = 'applied' | 'aborted' | 'validation' | 'conflict' | 'resource' | 'authorization';

// @public
export type FixtureSource = {
    readonly kind: 'create';
} | {
    readonly kind: 'docx';
    readonly sha256: string;
    readonly bytesRef: string;
};

// @public
export interface FixtureStep {
    // (undocumented)
    readonly baseRevision: number;
    // (undocumented)
    readonly expect: FixtureExpectation;
    readonly ops: readonly unknown[];
    // (undocumented)
    readonly origin: string;
}

// @public
export function flattenContentControls(children: readonly OoxmlNode[], maxDepth?: number): readonly OoxmlNode[];

// @public
export type FldCharType = 'begin' | 'separate' | 'end';

// @public
export function fldCharType(node: OoxmlNode): FldCharType | null;

// @public
export function fldSimpleInstr(node: OoxmlNode): string | undefined;

// @public
export function fnv1a32(value: string): number;

// @public
export function foldCase(text: string): string;

// @public
export interface FontPort {
    // (undocumented)
    readonly kind: 'font';
}

// @public
export type FontStyleKey = 'regular' | 'bold' | 'italic' | 'boldItalic';

// @public
export type FootnotePosition = 'pageBottom' | 'beneathText' | 'sectEnd' | 'docEnd';

// @public
export function formatNoteScopeId(noteKind: NoteKind, noteId: number): string;

// @public
export function formatOwnedRunIds(paragraph: Extract<OoxmlNode, {
    kind: 'paragraph';
}>): ReadonlySet<string>;

// @public
export const HARD_CEILINGS: ResourceLimits;

// @public
export function hardBreakAttributes(kind: 'line' | 'page'): OoxmlHardBreakNode['attributes'];

// @public
export type HardBreakKind = 'line' | 'page' | 'column' | 'other';

// @public
export function hardBreakKind(node: OoxmlHardBreakNode): HardBreakKind;

// @public
export function hardBreakText(node: OoxmlHardBreakNode): string;

// @public
export function hasAnyComment(pkg: OoxmlPackage): boolean;

// @public
export function hasBoundedSvgRoot(bytes: Uint8Array): boolean;

// @public
export function hasCommentPart(pkg: OoxmlPackage, storyPartName: string): boolean;

// @public
export function hashAuthored(authoredState: unknown): string;

// @public
export function hasLegacyFormFieldData(node: OoxmlNode): boolean;

// @public
export function hasNode(part: OoxmlPart, nodeId: string): boolean;

// @public
export type HeaderFooterKind = 'header' | 'footer';

// @public
export type HeaderFooterLifecycleImpact = 'flow-structural' | 'global';

// @public
export type HeaderFooterLifecycleOp = {
    readonly op: 'createHeaderFooter';
    readonly sectionIndex: number;
    readonly kind: HeaderFooterKind;
    readonly variant: HeaderFooterVariant;
    readonly titlePage?: boolean;
    readonly evenAndOddHeaders?: boolean;
} | {
    readonly op: 'deleteHeaderFooter';
    readonly sectionIndex: number;
    readonly kind: HeaderFooterKind;
    readonly variant: HeaderFooterVariant;
} | {
    readonly op: 'linkToPrevious';
    readonly sectionIndex: number;
    readonly kind: HeaderFooterKind;
    readonly variant: HeaderFooterVariant;
} | {
    readonly op: 'unlinkFromPrevious';
    readonly sectionIndex: number;
    readonly kind: HeaderFooterKind;
    readonly variant: HeaderFooterVariant;
} | {
    readonly op: 'setSectionFurnitureOptions';
    readonly sectionIndex?: number;
    readonly titlePage?: boolean;
    readonly evenAndOddHeaders?: boolean;
    readonly headerDistanceTwips?: number;
    readonly footerDistanceTwips?: number;
};

// @public
export type HeaderFooterLifecycleRejection = 'invalidArgs' | 'tree-invariant';

// @public
export type HeaderFooterLifecycleResult = {
    readonly ok: true;
    readonly package: OoxmlPackage;
    readonly impact: HeaderFooterLifecycleImpact;
    readonly createdRId?: string;
    readonly createdPartName?: string;
} | {
    readonly ok: false;
    readonly reason: HeaderFooterLifecycleRejection;
    readonly detail?: string;
};

// @public
export interface HeaderFooterParts {
    readonly evenAndOddHeaders: boolean;
    // (undocumented)
    readonly footers: ReadonlyMap<HeaderFooterVariant, OoxmlPart>;
    // (undocumented)
    readonly headers: ReadonlyMap<HeaderFooterVariant, OoxmlPart>;
    readonly titlePage: boolean;
}

// @public
export interface HeaderFooterSectionResolution {
    // (undocumented)
    readonly evenAndOddHeaders: boolean;
    // (undocumented)
    readonly footers: ReadonlyMap<HeaderFooterVariant, HeaderFooterSlotMeta>;
    // (undocumented)
    readonly headers: ReadonlyMap<HeaderFooterVariant, HeaderFooterSlotMeta>;
    // (undocumented)
    readonly titlePage: boolean;
}

// @public
export interface HeaderFooterSlotMeta {
    // (undocumented)
    readonly inherited: boolean;
    // (undocumented)
    readonly part: OoxmlPart;
    // (undocumented)
    readonly partName: string;
    // (undocumented)
    readonly rId: string;
}

// @public
export type HeaderFooterVariant = 'default' | 'first' | 'even';

// @public
export type HrefProjection = {
    readonly ok: true;
    readonly href: string;
} | {
    readonly ok: false;
    readonly inert: true;
};

// @public
export const HYPERLINK_RELATIONSHIP_TYPE = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/hyperlink";

// @public
export function hyperlinkAnchorOf(link: OoxmlNode): string | undefined;

// @public
export type HyperlinkKind =
/** `r:id` → a relationship with `TargetMode="External"`. Opens somewhere else. */
'external'
/** `w:anchor` → a bookmark in this document. Scrolls, never navigates. */
| 'internal'
/**
* `r:id` naming a relationship the part does not declare, or one that is not external.
*
* The link keeps its runs — the text is never lost twice — but there is nothing to
* activate, so it paints inert exactly like a refused scheme.
*/
| 'unresolved';

// @public
export function hyperlinkRelationshipIdOf(link: OoxmlNode): string | undefined;

// @public
export interface HyperlinkTarget {
    readonly anchor?: string;
    readonly authored: string;
    readonly href: string | null;
    // (undocumented)
    readonly kind: HyperlinkKind;
    readonly relationshipId?: string;
    readonly tooltip?: string;
}

// @public
export function hyperlinkTargetOf(link: OoxmlNode, resolve: RelationshipTargetResolver): HyperlinkTarget;

// @public
export const ID_KINDS: readonly ["extension", "capability", "command", "query", "schema", "dependencyKey", "runtimePort", "result", "origin"];

// @public
export interface IdentityPort {
    // (undocumented)
    newId(): string;
}

// @public
export type IdKind = (typeof ID_KINDS)[number];

// @public
export const IMAGE_RELATIONSHIP_TYPE = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/image";

// @public
export const IMAGE_RESOURCE_HARD_CEILINGS: ImageResourceLimits;

// @public
export const IMAGE_WRAP_TARGETS: readonly ImageWrapTarget[];

// @public
export interface ImageDecodePort {
    convertPreserved?(bytes: Uint8Array, mime: PreservedImageMime, limits: ImageResourceLimits): Promise<Readonly<{
        bytes: Uint8Array;
        mime: SupportedImageMime;
    }> | null>;
    // (undocumented)
    decode(bytes: Uint8Array, mime: SupportedImageMime, limits: ImageResourceLimits): Promise<Readonly<{
        pixelWidth: number;
        pixelHeight: number;
        dpiX: number;
        dpiY: number;
    }>>;
}

// @public
export interface ImagePort {
    // (undocumented)
    readonly kind: 'image';
}

// @public
export type ImageRelationshipResolution = {
    readonly mode: 'internal';
    readonly partName: string;
    readonly raw: string;
} | {
    readonly mode: 'external';
    readonly sinkSafe: boolean;
    readonly raw: string;
} | {
    readonly mode: 'missing';
};

// @public
export interface ImageResourceLimits {
    // (undocumented)
    readonly maxDecodedBytes: number;
    // (undocumented)
    readonly maxDimension: number;
    // (undocumented)
    readonly maxEncodedBytes: number;
    // (undocumented)
    readonly maxExternalRedirects: number;
    // (undocumented)
    readonly maxPixels: number;
    // (undocumented)
    readonly maxPolygonPoints: number;
}

// @public
export interface ImageResourceLookup {
    // (undocumented)
    readonly dispose: () => void;
    // (undocumented)
    readonly liveReferenceCount: (partName: string) => number;
    // (undocumented)
    readonly resolveEmbedded: (ownerPartName: string, relationshipId: string) => Promise<ImageResourceState>;
    // (undocumented)
    readonly resolveForProjection: (projection: DrawingProjection) => Promise<ImageResourceState>;
    // (undocumented)
    readonly resolveLinked: (ownerPartName: string, relationshipId: string) => ImageResourceState;
}

// @public
export function imageResourceLookupFor(pkg: OoxmlPackage, options: CreateImageResourceCacheOptions): ImageResourceLookup;

// @public
export type ImageResourceState = {
    readonly kind: 'ready';
    readonly partName: string;
    readonly contentId: string;
    readonly resourceKey: string;
    readonly validatedHandle: ValidatedImageBytesHandle;
    readonly mime: RenderableImageMime;
    readonly pixelWidth: number;
    readonly pixelHeight: number;
    readonly dpiX: number;
    readonly dpiY: number;
} | {
    readonly kind: 'unrenderable';
    readonly partName: string | null;
    readonly mime: RenderableImageMime | PreservedImageMime | 'unknown';
    readonly reason: 'unsupported-format' | 'non-picture-graphic' | 'signature-mismatch' | 'decode-failed' | 'resource-limit';
} | {
    readonly kind: 'external';
    readonly relationshipId: string;
    readonly sinkSafe: boolean;
} | {
    readonly kind: 'missing';
    readonly relationshipId: string;
} | {
    readonly kind: 'pending';
    readonly resourceKey: string;
};

// @public
export type ImageWrapTarget = 'inline' | 'square' | 'squareLeft' | 'squareRight' | 'tight' | 'through' | 'topAndBottom' | 'behind' | 'inFront';

// @public
export type ImpactClass = 'text-local' | 'paragraph-local' | 'flow-structural' | 'global';

// @public
export type IndexResult = {
    readonly ok: true;
    readonly index: ContentTypeIndex;
} | {
    readonly ok: false;
    readonly error: ContentTypeError;
};

// @public
export function indexStyles(part: OoxmlPart | undefined): Map<string, StyleIndexEntry>;

// @public
export const INERT_EXECUTABLE_KINDS: readonly ["field-dde", "field-include", "macro", "activex", "ole", "embedded-object", "executable-relationship"];

// @public
export type InertExecutableKind = (typeof INERT_EXECUTABLE_KINDS)[number];

// @public
export function inlineControlEndingAt(paragraph: OoxmlParagraphNode, offset: number): InlineControlSpan | null;

// @public
export interface InlineControlSpan {
    // (undocumented)
    readonly controlId: string;
    // (undocumented)
    readonly end: number;
    // (undocumented)
    readonly start: number;
}

// @public
export function inlineControlStartingAt(paragraph: OoxmlParagraphNode, offset: number): InlineControlSpan | null;

// @public
export function insertChildren(part: OoxmlPart, nodeId: string, index: number, children: readonly OoxmlNode[], options?: EditOptions): OoxmlEditResult;

// @public
export interface InsertCustomNodeWrite {
    // (undocumented)
    readonly alias?: string;
    readonly lock?: 'sdtLocked' | 'sdtContentLocked' | 'contentLocked';
    // (undocumented)
    readonly offset: number;
    // (undocumented)
    readonly paragraphId: string;
    readonly payload?: CustomNodePayloadWrite;
    readonly replaceControlId?: string;
    readonly replaceUntil?: number;
    // (undocumented)
    readonly tag: string;
    // (undocumented)
    readonly text: string;
}

// @public
export function insertCustomNodeWrite(store: TreeDocumentStore, write: InsertCustomNodeWrite,
dataOwnerPartName?: string): CustomNodeWriteResult;

// @public
export function instrTextValue(node: OoxmlNode): string;

// @public
export function isAuthorableRunProperty(localName: string): boolean;

// @public
export function isContentControl(node: OoxmlNode): node is ContentControlLike;

// @public
export function isContentControlContent(node: OoxmlNode): node is ContentControlContentLike;

// @public
export function isContentControlContentNode(node: OoxmlNode): node is OoxmlContentControlContentNode;

// @public
export function isContentControlNode(node: OoxmlNode): node is OoxmlContentControlNode;

// @public
export function isContentControlWrapper(node: OoxmlNode): boolean;

// @public
export function isContentRevisionKind(kind: OoxmlNode['kind']): kind is 'revisionInsert' | 'revisionDelete' | 'revisionMoveFrom' | 'revisionMoveTo';

// @public
export function isContinuationSeparatorNode(node: OoxmlNode): node is OoxmlContinuationSeparatorNode;

// @public
export function isDangerousKey(key: string): boolean;

// @public
export function isDrawingTreeDocOp(op: TreeDocOp): op is DrawingTreeDocOp;

// @public
export function isEndnotesNode(node: OoxmlNode): node is OoxmlEndnotesNode;

// @public
export function isEvaluableField(instruction: string): boolean;

// @public
export function isFieldChrome(node: OoxmlNode): boolean;

// @public
export function isFldChar(node: OoxmlNode, type: FldCharType): boolean;

// @public
export function isFldCharNode(node: OoxmlNode): node is OoxmlFldCharNode;

// @public
export function isFldSimple(node: OoxmlNode): boolean;

// @public
export function isFldSimpleNode(node: OoxmlNode): node is OoxmlFldSimpleNode;

// @public
export function isFootnotesNode(node: OoxmlNode): node is OoxmlFootnotesNode;

// @public
export function isHeaderFooterLifecycleOp(op: {
    readonly op: string;
}): op is HeaderFooterLifecycleOp;

// @public
export function isHyperlinkNode(node: OoxmlNode): boolean;

// @public
export function isInertExecutable(kind: string): boolean;

// @public
export function isInstrText(node: OoxmlNode): boolean;

// @public
export function isInstrTextNode(node: OoxmlNode): node is OoxmlInstrTextNode;

// @public
export function isLegalEndnotePosition(pos: string): pos is EndnotePosition;

// @public
export function isLegalFldCharType(value: string): boolean;

// @public
export function isLegalFootnotePosition(pos: string): pos is FootnotePosition;

// @public
export function isLegalNumRestart(value: string): value is NoteNumRestart;

// @public
export function isNormalNote(node: OoxmlNode): boolean;

// @public
export function isNoteAtomNode(node: OoxmlNode): boolean;

// @public
export function isNoteLifecycleOp(op: {
    readonly op: string;
}): op is NoteLifecycleOp;

// @public
export function isNoteNode(node: OoxmlNode): node is OoxmlNoteNode;

// @public
export function isNoteReferenceNode(node: OoxmlNode): node is OoxmlNoteReferenceNode;

// @public
export function isNoteRefNode(node: OoxmlNode): node is OoxmlNoteRefNode;

// @public
export function isPageBreakNode(node: OoxmlNode): node is OoxmlHardBreakNode;

// @public
export function isRangeMarkerKind(kind: OoxmlNode['kind']): kind is 'moveFromRangeStart' | 'moveFromRangeEnd' | 'moveToRangeStart' | 'moveToRangeEnd' | 'commentRangeStart' | 'commentRangeEnd';

// @public
export function isSearchableQuery(query: unknown): query is string;

// @public
export function isSeparatorNode(node: OoxmlNode): node is OoxmlSeparatorNode;

// @public
export function isValidId(id: string): boolean;

// @public
export function isValidMime(value: string): boolean;

// @public
export function isValidNCName(name: string): boolean;

// @public
export function isValidParaId(value: string): boolean;

// @public
export function isValidQName(name: string): boolean;

// @public
export function isWholeWord(text: string, start: number, end: number): boolean;

// @public
export type Json = null | boolean | number | string | Json[] | {
    [k: string]: Json;
};

// @public
export const LIMIT_KEYS: (keyof ResourceLimits)[];

// @public
export const LIMIT_SPECS: Readonly<Record<keyof ResourceLimits, LimitSpec>>;

// @public
export class LimitExceededError extends Error {
    constructor(label: string, limit: number, attempted: number);
    // (undocumented)
    readonly attempted: number;
    // (undocumented)
    readonly label: string;
    // (undocumented)
    readonly limit: number;
}

// @public
export interface LimitSpec {
    // (undocumented)
    readonly phase: EnforcementPhase;
    // (undocumented)
    readonly unit: LimitUnit;
}

// @public
export type LimitUnit = 'bytes' | 'count' | 'depth' | 'ratio' | 'passes';

// @public
export interface LinkableReviewItem {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly kind: string;
    readonly nesting?: number;
    // (undocumented)
    readonly orphaned?: boolean;
    // (undocumented)
    readonly parentId?: string;
    // (undocumented)
    readonly parentRevisionId?: string;
    // (undocumented)
    readonly range?: ReviewRange | null;
    // (undocumented)
    readonly ranges?: readonly ReviewRange[];
    // (undocumented)
    readonly replyIds?: readonly string[];
}

// @public
export function linkRevisionReplies<T extends LinkableReviewItem>(items: readonly T[]): T[];

// @public
export type ListKind = 'bullet' | 'ordered';

// @public
export function liveDrawingReferenceCount(pkg: OoxmlPackage, partName: string): number;

// @public
export function locateSites(part: OoxmlPart): ReadonlyMap<string, SiteLocation>;

// @public
export function lockForbidsEdit(lock: ContentControlLock): boolean;

// @public
export function lockForbidsRemoval(lock: ContentControlLock): boolean;

// @public
export function makeLimitCounter(limits: ResourceLimits, key: keyof ResourceLimits): BoundedCounter;

// @public
export const MAX_CONTENT_CONTROL_NESTING = 32;

// @public
export const MAX_CONTENT_CONTROLS_PER_PART = 10000;

// @public
export const MAX_CUSTOM_NODE_LABEL_LENGTH = 4096;

// @public
export const MAX_CUSTOM_NODE_PAYLOAD_LENGTH: number;

// @public
export const MAX_NOTE_REFERENCE_PARTS = 256;

// @public
export const MAX_NOTE_REFERENCE_SCAN = 20000;

// @public
export const MAX_NOTES_PER_PART = 10000;

// @public
export const MAX_STORY_SDT_NESTING = 32;

// @public
export const MAX_SVG_SNIFF_BYTES = 512;

// @public
export function mergedProperties(existing: readonly OoxmlProperty[], incoming: OoxmlProperty | readonly OoxmlProperty[]): OoxmlProperty[];

// @public
export function mintedParagraphIdentityAttributes(prefix: string, value: string): readonly OoxmlAttribute[];

// @public
export function mintParaId(seed: string, used: ReadonlySet<string>): string;

// @public
export interface ModelChangeSummary {
    // (undocumented)
    readonly dependencyKeys: readonly string[];
    // (undocumented)
    readonly dirty: readonly string[];
    // (undocumented)
    readonly fromRevision: number;
    // (undocumented)
    readonly origin: string;
    // (undocumented)
    readonly toRevision: number;
}

// @public
export type NameRejection = 'empty' | 'control-char' | 'backslash' | 'drive-or-unc' | 'encoded-separator' | 'encoded-dot' | 'bad-encoding' | 'empty-segment' | 'dot-segment' | 'unsafe-key' | 'segment-trailing-dot' | 'traversal-escape' | 'not-absolute-uri' | 'unsafe-scheme';

// @public
export type NameResult = {
    readonly ok: true;
    readonly partName: string;
} | {
    readonly ok: false;
    readonly reason: NameRejection;
};

// @public
export const NO_TRACKING_SETTINGS: DocumentTrackingSettings;

// @public
export const NON_CANONICAL_ORIGINS: readonly string[];

// @public
export function normalizeParagraphIdentity(part: OoxmlPart): OoxmlPart;

// @public
export function normalizePartName(raw: string): NameResult;

// @public
export function normalNoteIds(part: OoxmlPart): readonly number[];

// @public
export const NOTE_ATOM_CHAR = "\uFFFC";

// @public
export const NOTE_CONTINUATION_SEPARATOR_ID = 0;

// @public
export const NOTE_ID_MAX = 2147483647;

// @public
export const NOTE_ID_MIN = 1;

// @public
export const NOTE_SEPARATOR_ID = -1;

// @public
export function noteAtomText(): typeof NOTE_ATOM_CHAR;

// @public
export type NoteDiagnostic = {
    readonly code: 'dangling-note-reference';
    readonly noteKind: NoteKind;
    readonly noteId: number;
    readonly sourceNodeId?: string;
} | {
    readonly code: 'note-reference-scan-truncated';
};

// @public
export type NoteDiagnosticCode = 'dangling-note-reference' | 'note-reference-scan-truncated';

// @public
export function noteIdOf(node: OoxmlNode): number | null;

// @public
export type NoteKind = 'footnote' | 'endnote';

// @public
export function noteKindOf(node: OoxmlNode): NoteKind | null;

// @public
export type NoteLifecycleImpact = 'flow-structural' | 'global';

// @public
export type NoteLifecycleOp = {
    readonly op: 'insertNote';
    readonly noteKind: NoteKind;
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'deleteNote';
    readonly noteKind: NoteKind;
    readonly noteId: number;
} | {
    readonly op: 'convertNote';
    readonly fromKind: NoteKind;
    readonly noteId: number;
} | {
    readonly op: 'convertAllNotes';
    readonly fromKind: NoteKind;
} | {
    readonly op: 'setNoteProperties';
    readonly scope: 'document' | 'section';
    readonly sectionIndex?: number;
    readonly footnote?: {
        readonly numFmt?: string;
        readonly numRestart?: string;
        readonly position?: string;
        readonly numStart?: number;
    };
    readonly endnote?: {
        readonly numFmt?: string;
        readonly numRestart?: string;
        readonly position?: string;
        readonly numStart?: number;
    };
};

// @public
export interface NoteLifecycleOptions {
    readonly scanBudget?: NoteReferenceScanBudget;
}

// @public
export type NoteLifecycleRejection = 'invalidArgs' | 'tree-invariant';

// @public
export type NoteLifecycleResult = {
    readonly ok: true;
    readonly package: OoxmlPackage;
    readonly impact: NoteLifecycleImpact;
    readonly noteId?: number;
    readonly noteKind?: NoteKind;
    readonly createdPartName?: string;
} | {
    readonly ok: false;
    readonly reason: NoteLifecycleRejection;
    readonly detail?: string;
};

// @public
export type NoteNumRestart = 'continuous' | 'eachSect' | 'eachPage';

// @public
export interface NoteReferenceHit {
    readonly atomOffset: number;
    // (undocumented)
    readonly customMarkFollows: boolean;
    // (undocumented)
    readonly nodeId: string;
    // (undocumented)
    readonly noteId: number;
    // (undocumented)
    readonly noteKind: NoteKind;
    // (undocumented)
    readonly paragraphId: string;
    readonly partName: string;
}

// @public
export function noteReferenceKindOf(node: OoxmlNode): NoteKind | null;

// @public
export interface NoteReferenceScanBudget {
    // (undocumented)
    readonly maxParts: number;
    // (undocumented)
    readonly maxVisited: number;
    // (undocumented)
    parts: number;
    truncated: boolean;
    // (undocumented)
    visited: number;
}

// @public
export function noteRefKindOf(node: OoxmlNode): NoteKind | null;

// @public
export function notesOf(root: OoxmlNode): readonly OoxmlNoteNode[];

// @public
export function notesPartHasId(part: OoxmlPart, noteId: number): boolean;

// @public
export type NoteType = 'normal' | 'separator' | 'continuationSeparator' | 'continuationNotice';

// @public
export function noteTypeOf(node: OoxmlNode): NoteType | undefined;

// @public
export function nullRecord<T = unknown>(): Record<string, T>;

// @public
export const OFFICE_RELATIONSHIP_NAMESPACE_URI = "http://schemas.openxmlformats.org/officeDocument/2006/relationships";

// @public
export interface OffsetSpan {
    // (undocumented)
    readonly end: number;
    // (undocumented)
    readonly start: number;
}

// @public
export const OOXML_NODE_IDENTITY_RULES: OoxmlNodeIdentityRules;

// @public
export type OoxmlAttribute = OoxmlXmlSpaceAttribute | OoxmlWmlValAttribute | OoxmlGenericExtensionAttribute;

// @public
export interface OoxmlBodyNode extends OoxmlElementBase<readonly (OoxmlParagraphNode | OoxmlTableNode | OoxmlContentControlNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'body';
    // (undocumented)
    readonly localName: 'body';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlBookmarkEndNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'bookmarkEnd';
    // (undocumented)
    readonly localName: 'bookmarkEnd';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlBookmarkStartNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'bookmarkStart';
    // (undocumented)
    readonly localName: 'bookmarkStart';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlCalendarNode extends OoxmlElementBase<readonly [], readonly (OoxmlKnownNodeAttribute | OoxmlWmlValAttribute)[]> {
    // (undocumented)
    readonly kind: 'contentControlCalendar';
    // (undocumented)
    readonly localName: 'calendar';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlCheckboxNode extends OoxmlElementBase<readonly (OoxmlContentControlCheckedNode | OoxmlContentControlCheckedStateNode | OoxmlContentControlUncheckedStateNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlCheckbox';
    // (undocumented)
    readonly localName: 'checkbox';
    // (undocumented)
    readonly namespaceUri: typeof W14_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlCheckedNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlChecked';
    // (undocumented)
    readonly localName: 'checked';
    // (undocumented)
    readonly namespaceUri: typeof W14_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlCheckedStateNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlCheckedState';
    // (undocumented)
    readonly localName: 'checkedState';
    // (undocumented)
    readonly namespaceUri: typeof W14_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlComboBoxNode extends OoxmlElementBase<readonly (OoxmlContentControlListItemNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlComboBox';
    // (undocumented)
    readonly localName: 'comboBox';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlContentNode extends OoxmlElementBase<readonly OoxmlNode[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlContent';
    // (undocumented)
    readonly localName: 'sdtContent';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlDataBindingNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlDataBinding';
    // (undocumented)
    readonly localName: 'dataBinding';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlDateFormatNode extends OoxmlElementBase<readonly [], readonly (OoxmlKnownNodeAttribute | OoxmlWmlValAttribute)[]> {
    // (undocumented)
    readonly kind: 'contentControlDateFormat';
    // (undocumented)
    readonly localName: 'dateFormat';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlDateNode extends OoxmlElementBase<readonly (OoxmlContentControlDateFormatNode | OoxmlContentControlLidNode | OoxmlContentControlStoreMappedDataAsNode | OoxmlContentControlCalendarNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlDate';
    // (undocumented)
    readonly localName: 'date';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlDropDownListNode extends OoxmlElementBase<readonly (OoxmlContentControlListItemNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlDropDownList';
    // (undocumented)
    readonly localName: 'dropDownList';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlEndPropertiesNode extends OoxmlElementBase<readonly (OoxmlRunPropertiesNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlEndProperties';
    // (undocumented)
    readonly localName: 'sdtEndPr';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlLidNode extends OoxmlElementBase<readonly [], readonly (OoxmlKnownNodeAttribute | OoxmlWmlValAttribute)[]> {
    // (undocumented)
    readonly kind: 'contentControlLid';
    // (undocumented)
    readonly localName: 'lid';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlListItemNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlListItem';
    // (undocumented)
    readonly localName: 'listItem';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlNode extends OoxmlElementBase<readonly (OoxmlContentControlPropertiesNode | OoxmlContentControlEndPropertiesNode | OoxmlContentControlContentNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControl';
    // (undocumented)
    readonly localName: 'sdt';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlPropertiesNode extends OoxmlElementBase<readonly (OoxmlRunPropertiesNode | OoxmlContentControlDataBindingNode | OoxmlContentControlDropDownListNode | OoxmlContentControlComboBoxNode | OoxmlContentControlDateNode | OoxmlContentControlTextNode | OoxmlContentControlCheckboxNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlProperties';
    // (undocumented)
    readonly localName: 'sdtPr';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlStoreMappedDataAsNode extends OoxmlElementBase<readonly [], readonly (OoxmlKnownNodeAttribute | OoxmlWmlValAttribute)[]> {
    // (undocumented)
    readonly kind: 'contentControlStoreMappedDataAs';
    // (undocumented)
    readonly localName: 'storeMappedDataAs';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlTextNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlText';
    // (undocumented)
    readonly localName: 'text';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlContentControlUncheckedStateNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'contentControlUncheckedState';
    // (undocumented)
    readonly localName: 'uncheckedState';
    // (undocumented)
    readonly namespaceUri: typeof W14_NAMESPACE_URI;
}

// @public
export interface OoxmlContinuationSeparatorNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'continuationSeparator';
    // (undocumented)
    readonly localName: 'continuationSeparator';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlDocumentNode extends OoxmlElementBase<readonly (OoxmlBodyNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'document';
    // (undocumented)
    readonly localName: 'document';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export type OoxmlEditResult = {
    readonly ok: true;
    readonly part: OoxmlPart;
} | {
    readonly ok: false;
    readonly issues: readonly OoxmlInvariantIssue[];
};

// @public
export type OoxmlElement = OoxmlDocumentNode | OoxmlBodyNode | OoxmlParagraphNode | OoxmlRunNode | OoxmlHyperlinkNode | OoxmlBookmarkStartNode | OoxmlBookmarkEndNode | OoxmlRunPropertiesNode | OoxmlTextElementNode | OoxmlDeletedTextNode | OoxmlParagraphPropertiesNode | OoxmlTabNode | OoxmlHardBreakNode | OoxmlFldCharNode | OoxmlInstrTextNode | OoxmlFldSimpleNode | OoxmlFootnotesNode | OoxmlEndnotesNode | OoxmlNoteNode | OoxmlNoteReferenceNode | OoxmlNoteRefNode | OoxmlSeparatorNode | OoxmlContinuationSeparatorNode | OoxmlTableNode | OoxmlTableRowNode | OoxmlTableCellNode | OoxmlTableGridNode | OoxmlTablePropertiesNode | OoxmlRevisionContentNode | OoxmlRangeMarkerNode | OoxmlCommentReferenceNode | OoxmlCommentsNode | OoxmlCommentNode | OoxmlContentControlNode | OoxmlContentControlPropertiesNode | OoxmlContentControlEndPropertiesNode | OoxmlContentControlContentNode | OoxmlContentControlDropDownListNode | OoxmlContentControlComboBoxNode | OoxmlContentControlListItemNode | OoxmlContentControlDateNode | OoxmlContentControlDateFormatNode | OoxmlContentControlLidNode | OoxmlContentControlStoreMappedDataAsNode | OoxmlContentControlCalendarNode | OoxmlContentControlTextNode | OoxmlContentControlDataBindingNode | OoxmlContentControlCheckboxNode | OoxmlContentControlCheckedNode | OoxmlContentControlCheckedStateNode | OoxmlContentControlUncheckedStateNode | OoxmlDrawingNode | OoxmlInlineDrawingNode | OoxmlAnchoredDrawingNode | OoxmlDrawingExtentNode | OoxmlDrawingEffectExtentNode | OoxmlDrawingDocPrNode | OoxmlDrawingGraphicFramePrNode | OoxmlDrawingGraphicNode | OoxmlDrawingGraphicDataNode | OoxmlDrawingSimplePosNode | OoxmlDrawingPositionHNode | OoxmlDrawingPositionVNode | OoxmlDrawingPositionAlignNode | OoxmlDrawingPositionOffsetNode | OoxmlDrawingWrapNoneNode | OoxmlDrawingWrapSquareNode | OoxmlDrawingWrapTightNode | OoxmlDrawingWrapThroughNode | OoxmlDrawingWrapTopBottomNode | OoxmlDrawingWrapPolygonNode | OoxmlDrawingWrapPolygonStartNode | OoxmlDrawingWrapPolygonLineToNode | OoxmlPictureNode | OoxmlPictureNvPicPrNode | OoxmlPictureBlipFillNode | OoxmlPictureBlipNode | OoxmlPictureSrcRectNode | OoxmlPictureStretchNode | OoxmlPictureTileNode | OoxmlPictureShapePropertiesNode | OoxmlPictureTransformNode | OoxmlPictureTransformOffsetNode | OoxmlPictureTransformExtentNode | OoxmlPicturePresetGeometryNode | OoxmlGenericElementNode;

// @public
export interface OoxmlEndnotesNode extends OoxmlElementBase<readonly (OoxmlNoteNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'endnotes';
    // (undocumented)
    readonly localName: 'endnotes';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlExternalTarget {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly ownerPart: string;
    // (undocumented)
    readonly rawTarget: string;
    readonly sinkSafe: boolean;
    // (undocumented)
    readonly type: string;
}

// @public
export interface OoxmlFldCharNode extends OoxmlElementBase<readonly OoxmlGenericElementNode[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'fldChar';
    // (undocumented)
    readonly localName: 'fldChar';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlFldSimpleNode extends OoxmlElementBase<readonly (OoxmlRunNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'fldSimple';
    // (undocumented)
    readonly localName: 'fldSimple';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlFootnotesNode extends OoxmlElementBase<readonly (OoxmlNoteNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'footnotes';
    // (undocumented)
    readonly localName: 'footnotes';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlGenericElementNode extends OoxmlElementBase<readonly OoxmlNode[]> {
    // (undocumented)
    readonly kind: 'generic';
}

// @public
export interface OoxmlGenericExtensionAttribute extends OoxmlAttributeBase {
    // (undocumented)
    readonly kind: 'genericExtension';
}

// @public
export interface OoxmlHardBreakNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'hardBreak';
    // (undocumented)
    readonly localName: 'br';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlHyperlinkNode extends OoxmlElementBase<readonly (OoxmlRunNode | OoxmlHyperlinkNode | OoxmlContentControlNode | OoxmlBookmarkStartNode | OoxmlBookmarkEndNode | OoxmlRevisionContentNode | OoxmlRangeMarkerNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'hyperlink';
    // (undocumented)
    readonly localName: 'hyperlink';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlIndexes {
    readonly paragraphs: ReadonlyMap<string, ParagraphIndexEntry>;
    // (undocumented)
    readonly relationships: ReadonlyMap<string, readonly RelationshipRecord[]>;
    readonly revision: number;
    readonly stories: ReadonlyMap<string, StoryIndexEntry>;
    // (undocumented)
    readonly styles: ReadonlyMap<string, StyleIndexEntry>;
}

// @public
export interface OoxmlInstrTextNode extends OoxmlElementBase<readonly OoxmlTextNode[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'instrText';
    // (undocumented)
    readonly localName: 'instrText';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlInvariantIssue {
    // (undocumented)
    readonly code: OoxmlInvariantIssueCode;
    // (undocumented)
    readonly nodeId?: string;
    // (undocumented)
    readonly path: string;
}

// @public
export type OoxmlInvariantIssueCode = 'invalid-id' | 'duplicate-id' | 'invalid-name' | 'invalid-namespace' | 'invalid-qname' | 'duplicate-expanded-attribute' | 'invalid-xml-value' | 'known-node-invariant';

// @public
export type OoxmlInvariantResult = {
    readonly ok: true;
} | {
    readonly ok: false;
    readonly issues: readonly OoxmlInvariantIssue[];
};

// @public
export type OoxmlKnownNodeAttribute = OoxmlXmlSpaceAttribute | OoxmlGenericExtensionAttribute;

// @public
export interface OoxmlNamespaceBinding {
    // (undocumented)
    readonly namespaceUri: string;
    // (undocumented)
    readonly prefix: string;
}

// @public
export type OoxmlNode = OoxmlElement | OoxmlTextNode;

// @public
export type OoxmlNodeId = string;

// @public
export interface OoxmlNodeIdentityRules {
    // (undocumented)
    readonly initial: 'deterministic-structural-path-after-normalized-parse';
    // (undocumented)
    readonly replacement: 'explicitly-retain-or-allocate';
    // (undocumented)
    readonly unchanged: 'retain-id-through-structural-sharing';
    // (undocumented)
    readonly uniqueness: 'unique-within-part';
}

// @public
export interface OoxmlNoteNode extends OoxmlElementBase<readonly (OoxmlParagraphNode | OoxmlTableNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'note';
    // (undocumented)
    readonly localName: 'footnote' | 'endnote';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlNoteReferenceNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'noteReference';
    // (undocumented)
    readonly localName: 'footnoteReference' | 'endnoteReference';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlNoteRefNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'noteRef';
    // (undocumented)
    readonly localName: 'footnoteRef' | 'endnoteRef';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlPackage {
    // (undocumented)
    readonly contentTypes: ContentTypeIndex;
    // (undocumented)
    readonly externalTargets: readonly OoxmlExternalTarget[];
    readonly mainDocumentPart: string;
    readonly partBytes: ReadonlyMap<string, Uint8Array>;
    readonly parts: ReadonlyMap<string, OoxmlPart>;
    readonly relationships: ReadonlyMap<string, readonly RelationshipRecord[]>;
}

// @public
export interface OoxmlPackageLimits {
    readonly maxRelationships?: number;
    readonly maxXmlParts?: number;
    // (undocumented)
    readonly xml?: XmlLimits;
    // (undocumented)
    readonly zip?: ZipLimits;
}

// @public
export type OoxmlPackageRejection = ZipRejection | OoxmlReadRejection | 'no-content-types' | 'bad-content-types' | 'no-main-document' | 'bad-relationship-target' | 'duplicate-relationship-id' | 'too-many-relationships' | 'too-many-xml-parts';

// @public
export type OoxmlPackageResult = {
    readonly ok: true;
    readonly package: OoxmlPackage;
} | {
    readonly ok: false;
    readonly reason: OoxmlPackageRejection;
    readonly detail?: string;
};

// @public
export interface OoxmlParagraphNode extends OoxmlElementBase<readonly (OoxmlParagraphPropertiesNode | OoxmlRunNode | OoxmlHyperlinkNode | OoxmlContentControlNode | OoxmlBookmarkStartNode | OoxmlBookmarkEndNode | OoxmlRevisionContentNode | OoxmlRangeMarkerNode | OoxmlFldSimpleNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'paragraph';
    // (undocumented)
    readonly localName: 'p';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlParagraphPropertiesNode extends OoxmlElementBase<readonly (OoxmlRunPropertiesNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'paragraphProperties';
    // (undocumented)
    readonly localName: 'pPr';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlPart {
    // (undocumented)
    readonly contentType: string;
    // (undocumented)
    readonly id: string;
    readonly name: string;
    // (undocumented)
    readonly root: OoxmlElement;
}

// @public
export interface OoxmlPartMetadata {
    // (undocumented)
    readonly contentType: string;
    // (undocumented)
    readonly name: string;
}

// @public
export interface OoxmlProperty {
    // (undocumented)
    readonly attributes?: Readonly<Record<string, string>>;
    // (undocumented)
    readonly localName: string;
}

// @public
export type OoxmlReadRejection = XmlRejection | 'missing-root' | 'multiple-roots' | 'invalid-name' | 'invalid-namespace' | 'undeclared-prefix' | 'duplicate-expanded-attribute';

// @public
export type OoxmlReadResult = {
    readonly ok: true;
    readonly part: OoxmlPart;
} | {
    readonly ok: false;
    readonly reason: OoxmlReadRejection;
};

// @public
export interface OoxmlRunNode extends OoxmlElementBase<readonly (OoxmlRunPropertiesNode | OoxmlTextElementNode | OoxmlDeletedTextNode | OoxmlTabNode | OoxmlHardBreakNode | OoxmlCommentReferenceNode | OoxmlFldCharNode | OoxmlInstrTextNode | OoxmlNoteReferenceNode | OoxmlNoteRefNode | OoxmlSeparatorNode | OoxmlContinuationSeparatorNode | OoxmlDrawingNode | OoxmlGenericElementNode)[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'run';
    // (undocumented)
    readonly localName: 'r';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlRunPropertiesNode extends OoxmlElementBase<readonly OoxmlGenericElementNode[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'runProperties';
    // (undocumented)
    readonly localName: 'rPr';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlSeparatorNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'separator';
    // (undocumented)
    readonly localName: 'separator';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export type OoxmlStoryKind = 'body' | 'header' | 'footer' | 'note';

// @public
export interface OoxmlStoryRoot {
    // (undocumented)
    readonly kind: OoxmlStoryKind;
    readonly root: OoxmlNode;
}

// @public
export interface OoxmlTabNode extends OoxmlElementBase<readonly [], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'tab';
    // (undocumented)
    readonly localName: 'tab';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlTextElementNode extends OoxmlElementBase<readonly OoxmlTextNode[], readonly OoxmlKnownNodeAttribute[]> {
    // (undocumented)
    readonly kind: 'text';
    // (undocumented)
    readonly localName: 't';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlTextNode {
    // (undocumented)
    readonly id: OoxmlNodeId;
    // (undocumented)
    readonly kind: 'textValue';
    // (undocumented)
    readonly value: string;
}

// @public
export function ooxmlTreesEqual(left: OoxmlPart | OoxmlNode, right: OoxmlPart | OoxmlNode): boolean;

// @public
export interface OoxmlWmlValAttribute extends OoxmlAttributeBase {
    // (undocumented)
    readonly kind: 'wmlVal';
    // (undocumented)
    readonly localName: 'val';
    // (undocumented)
    readonly namespaceUri: typeof WML_NAMESPACE_URI;
}

// @public
export interface OoxmlXmlSpaceAttribute extends OoxmlAttributeBase {
    // (undocumented)
    readonly kind: 'xmlSpace';
    // (undocumented)
    readonly localName: 'space';
    // (undocumented)
    readonly namespaceUri: typeof XML_NAMESPACE_URI;
    // (undocumented)
    readonly prefix: 'xml';
    // (undocumented)
    readonly value: 'default' | 'preserve';
}

// @public
export interface OperationContext {
    // (undocumented)
    readonly budget: Budget;
    // (undocumented)
    readonly cancellation: CancellationController;
    // (undocumented)
    readonly config: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly limits: ResourceLimits;
    // (undocumented)
    readonly ports: PortRegistry;
}

// @public
export interface OperationInit {
    readonly capacity?: number;
    // (undocumented)
    readonly config?: Readonly<Record<string, unknown>>;
    readonly id?: string;
    // (undocumented)
    readonly limits?: Partial<ResourceLimits>;
    // (undocumented)
    readonly ports: PortRegistry;
}

// @public
export function orderedContentControlProperties(children: readonly OoxmlNode[]): readonly OoxmlNode[];

// @public
export const ORIGIN_IDS: {
    readonly mutationHuman: "dev.docx-editor.core.origin.mutation.human";
    readonly mutationAgent: "dev.docx-editor.core.origin.mutation.agent";
    readonly mutationRemote: "dev.docx-editor.core.origin.mutation.remote";
    readonly mutationUndo: "dev.docx-editor.core.origin.mutation.undo";
    readonly mutationRedo: "dev.docx-editor.core.origin.mutation.redo";
    readonly mutationMigration: "dev.docx-editor.core.origin.mutation.migration";
    readonly mutationRepair: "dev.docx-editor.core.origin.mutation.repair";
    readonly mutationServer: "dev.docx-editor.core.origin.mutation.server";
    readonly projection: "dev.docx-editor.core.origin.projection";
    readonly awareness: "dev.docx-editor.core.origin.awareness";
};

// @public
export interface OverrideRecord {
    // (undocumented)
    readonly contentType: string;
    // (undocumented)
    readonly order: number;
    // (undocumented)
    readonly partName: string;
}

// @public
export type PackageInvariantCode = 'dangling-relationship' | 'missing-content-type'
/** A part occupies a name OPC reserves for package infrastructure. */
| 'reserved-part-name'
/** Two parts whose names differ only by case, which OPC treats as one part. */
| 'duplicate-part-name'
/** A part name the OPC screens refuse; `writeZip` would throw on save. */
| 'unsafe-part-name';

// @public
export interface PackageInvariantIssue {
    // (undocumented)
    readonly code: PackageInvariantCode;
    readonly ownerPart?: string;
    readonly partName: string;
}

// @public
export type PackageInvariantResult = {
    readonly ok: true;
} | {
    readonly ok: false;
    readonly issues: readonly PackageInvariantIssue[];
};

// @public
export type PackageTransactResult = {
    readonly ok: true;
    readonly change: TreeModelChange | null;
} | {
    readonly ok: false;
    readonly reason: StoryTargetRejection | TreeOpRejection;
    readonly detail?: string;
};

// @public
export const PAGE_BREAK_CHAR = "\f";

// @public
export interface ParagraphDigest {
    readonly genericStructure: readonly string[];
    readonly ordinal: number;
    readonly paragraphProperties: readonly string[];
    readonly paraId: string | null;
    readonly path: string;
    readonly runProperties: readonly (readonly string[])[];
    readonly text: string;
}

// @public
export interface ParagraphIndexEntry {
    readonly nodeId: string;
    readonly ordinal: number;
    readonly runIds: readonly string[];
    readonly text: string;
}

// @public
export interface ParagraphOffsetIndex {
    readonly length: number;
    lengthOf(node: OoxmlNode | string): number;
    // (undocumented)
    readonly segments: readonly Segment[];
    spanOf(node: OoxmlNode | string): OffsetSpan | null;
}

// @public
export function paragraphOffsetIndex(paragraph: OoxmlParagraphNode): ParagraphOffsetIndex;

// @public
export function paragraphOrderOfPart(part: OoxmlPart): ReadonlyMap<string, number>;

// @public
export function paragraphTextOf(part: OoxmlPart, paragraphId: string): string | null;

// @public
export function paraIdOf(node: OoxmlNode): string | null;

// @public
export function parentNodeOf(part: OoxmlPart, nodeId: string): OoxmlElement | null;

// @public
export function parseAuthoredNoteProperties(propsNode: OoxmlNode | null | undefined): AuthoredNoteProperties | undefined;

// @public
export function parseContentControlId(raw: string | undefined): number | undefined;

// @public
export function parseNoteId(raw: string | undefined): number | null;

// @public
export function parseNoteScopeId(id: string): {
    readonly noteKind: NoteKind;
    readonly noteId: number;
} | null;

// @public
export function parseSemVer(input: string): SemVer;

// @public
export function parseTocInstruction(raw: string): TocInstruction | null;

// @public
export function partNameKey(partName: string): string;

// @public
export interface PersistencePort {
    // (undocumented)
    readonly kind: 'persistence';
}

// @public
export function planTocEntries(part: OoxmlPart, outline: readonly TocOutlineHeading[], instruction: TocInstruction, pageNumberByParagraphId: ReadonlyMap<string, string>, excludeParagraphIds: ReadonlySet<string>): {
    readonly entries: readonly TocEntryPlan[];
    readonly bookmarksToCreate: readonly {
        paragraphId: string;
        name: string;
    }[];
};

// @public
export class PortRegistry {
    availablePorts(): string[];
    // (undocumented)
    clock(): ClockPort;
    // (undocumented)
    has(portId: string): boolean;
    // (undocumented)
    identity(): IdentityPort;
    missing(portIds: readonly string[]): string[];
    // (undocumented)
    provide(portId: string, port: unknown): this;
    resolve<T>(portId: string): T;
}

// @public
export class PortResolutionError extends Error {
    constructor(portId: string);
    // (undocumented)
    readonly portId: string;
}

// @public
export class PrefixAllocator {
    constructor(known?: Readonly<Record<string, string>>);
    bindings(): {
        prefix: string;
        uri: string;
    }[];
    // (undocumented)
    prefixFor(namespaceUri: string): string;
}

// @public
export type PreservedImageMime = 'image/tiff' | 'image/x-emf' | 'image/x-wmf';

// @public
export function projectDrawing(drawing: OoxmlDrawingNode, context: Readonly<{
    ownerPartName: string;
    supportedMcRequires: ReadonlySet<string>;
    limits: DrawingProjectionLimits;
    namespaceScope?: ReadonlyMap<string, string>;
    resolveRelationship?: RelationshipTargetResolver;
}>): DrawingProjection | null;

// @public
export function propertyContainer(parent: OoxmlNode | null | undefined, kind: 'paragraphProperties' | 'runProperties', localName: 'pPr' | 'rPr'): OoxmlNode | undefined;

// @public
export function readCustomXmlNode(pkg: OoxmlPackage, partName: string, nodeId: string): CustomXmlNode | null;

// @public
export function readEmbeddedFonts(pkg: OoxmlPackage, fontTable: OoxmlPart | undefined, options?: ReadEmbeddedFontsOptions): EmbeddedFont[];

// @public
export interface ReadEmbeddedFontsOptions {
    readonly maxFontBytes?: number;
    readonly maxFonts?: number;
}

// @public
export function readOnOffChild(parent: OoxmlNode, localName: string, namespaceUri?: string): boolean;

// @public
export function readOoxmlPackage(bytes: Uint8Array, limits?: OoxmlPackageLimits): OoxmlPackageResult;

// @public
export function readOoxmlPart(xml: string, metadata: OoxmlPartMetadata, limits?: XmlLimits): OoxmlReadResult;

// @public
export function readTrackingSettings(settingsRoot: OoxmlNode | null | undefined): DocumentTrackingSettings;

// @public
export function readViewSettings(settingsRoot: OoxmlNode | null | undefined): DocumentViewSettings;

// @public
export function readXml(xml: string, limits?: XmlLimits): XmlResult;

// @public
export function readZip(bytes: Uint8Array, limits?: ZipLimits): ZipReadResult;

// @public
export class RegistryError extends Error {
    constructor(code: RegistryErrorCode, message: string,
    responsible?: readonly string[]);
    // (undocumented)
    readonly code: RegistryErrorCode;
    readonly responsible: readonly string[];
}

// @public
export type RegistryErrorCode = 'invalid-id' | 'invalid-version' | 'duplicate-extension' | 'id-collision' | 'replacement-target-missing' | 'unauthorized-replacement' | 'replacement-version-mismatch' | 'ambiguous-replacement' | 'missing-dependency' | 'dependency-cycle' | 'conflict' | 'missing-port';

// @public
export type RelationshipError = {
    readonly code: 'duplicate-id';
    readonly ownerPart: string;
    readonly id: string;
};

// @public
export interface RelationshipRecord {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly order: number;
    // (undocumented)
    readonly ownerPart: string;
    // (undocumented)
    readonly rawTarget: string;
    // (undocumented)
    readonly targetMode: TargetMode;
    // (undocumented)
    readonly type: string;
}

// @public
export type RelationshipSetResult = {
    readonly ok: true;
    readonly byOwner: ReadonlyMap<string, readonly RelationshipRecord[]>;
} | {
    readonly ok: false;
    readonly error: RelationshipError;
};

// @public
export function relationshipsOf(pkg: OoxmlPackage, ownerPart: string): readonly RelationshipRecord[];

// @public
export function relationshipTargetIn(pkg: OoxmlPackage, ownerPart: string, relationshipId: string): {
    readonly target: string;
    readonly external: boolean;
    readonly sinkSafe?: boolean;
} | null;

// @public
export type RelationshipTargetResolver = (relationshipId: string) => {
    readonly target: string;
    readonly external: boolean;
    readonly sinkSafe?: boolean;
} | null;

// @public
export function relsPartNameFor(partName: string): string;

// @public
export function removeCustomNodeWrite(store: TreeDocumentStore, controlNodeId: string): CustomNodeWriteResult;

// @public
export function removeNode(part: OoxmlPart, nodeId: string, options?: EditOptions): OoxmlEditResult;

// @public
export function replaceChildren(part: OoxmlPart, nodeId: string, children: readonly OoxmlNode[], options?: EditOptions): OoxmlEditResult;

// @public
export type ReplacementPolicy = {
    readonly kind: 'none';
} | {
    readonly kind: 'single';
} | {
    readonly kind: 'priority';
};

// @public
export function replaceNode(part: OoxmlPart, nodeId: string, replacement: OoxmlNode, options?: EditOptions): OoxmlEditResult;

// @public
export function replayFixture(fixture: ConformanceFixture, store: ReplayStore): ReplayReport;

// @public
export interface ReplayOutcome {
    readonly authoredState?: unknown;
    // (undocumented)
    readonly committedRevision?: number;
    // (undocumented)
    readonly outcome: FixtureOutcome;
}

// @public
export interface ReplayReport {
    // (undocumented)
    readonly mismatches: readonly string[];
    // (undocumented)
    readonly ok: boolean;
}

// @public
export interface ReplayStore {
    applyStep(step: FixtureStep): ReplayOutcome;
    init(fixture: ConformanceFixture): number;
}

// @public
export interface Reservation {
    // (undocumented)
    readonly amount: number;
    // (undocumented)
    release(): void;
    // (undocumented)
    readonly released: boolean;
}

// @public
export function resolve(bundles: readonly FeatureBundle[], options?: ResolveOptions): ResolvedRegistry;

// @public
export function resolveContentControlLock(chain: readonly ContentControlLock[]): ContentControlLock;

// @public
export function resolveContentType(index: ContentTypeIndex, partName: string): ResolveResult;

// @public
export function resolveContentTypeOf(pkg: OoxmlPackage, partName: string): string | null;

// @public
export interface ResolvedEndnoteProperties {
    // (undocumented)
    readonly numFmt: string;
    // (undocumented)
    readonly numRestart: NoteNumRestart;
    // (undocumented)
    readonly numStart: number;
    // (undocumented)
    readonly pos: EndnotePosition;
}

// @public
export interface ResolvedFootnoteProperties {
    // (undocumented)
    readonly numFmt: string;
    // (undocumented)
    readonly numRestart: NoteNumRestart;
    // (undocumented)
    readonly numStart: number;
    // (undocumented)
    readonly pos: FootnotePosition;
}

// @public
export interface ResolvedRegistry {
    // (undocumented)
    readonly contributions: ReadonlyMap<string, Contribution>;
    // (undocumented)
    readonly extensions: ReadonlyMap<string, FeatureBundle>;
    // (undocumented)
    get(kind: Contribution['kind'], id: string): Contribution | undefined;
}

// @public
export type ResolvedRelationship = {
    readonly mode: 'Internal';
    readonly target: NameResult;
    readonly raw: string;
} | {
    readonly mode: 'External';
    readonly sinkSafe: NameResult;
    readonly raw: string;
};

// @public
export function resolveEndnoteProperties(section?: AuthoredNoteProperties, document?: AuthoredNoteProperties): ResolvedEndnoteProperties;

// @public
export function resolveFootnoteProperties(section?: AuthoredNoteProperties, document?: AuthoredNoteProperties): ResolvedFootnoteProperties;

// @public
export function resolveHeaderFooterParts(pkg: OoxmlPackage): HeaderFooterParts;

// @public
export function resolveHeaderFooterPartsBySection(pkg: OoxmlPackage): readonly HeaderFooterParts[];

// @public
export function resolveHeaderFooterResolutionBySection(pkg: OoxmlPackage): readonly HeaderFooterSectionResolution[];

// @public
export function resolveImageRelationship(records: readonly RelationshipRecord[] | undefined, ownerPart: string, relationshipId: string): ImageRelationshipResolution;

// @public
export function resolveImageResourceLimits(overrides?: Partial<ImageResourceLimits>): ImageResourceLimits;

// @public
export function resolveInternalTarget(ownerPartName: string, rawTarget: string): NameResult;

// @public
export function resolveLimits(overrides?: Partial<ResourceLimits>): ResourceLimits;

// @public
export function resolveNotesPart(pkg: OoxmlPackage, noteKind: NoteKind): OoxmlPart | null;

// @public
export interface ResolveOptions {
    readonly availablePorts?: readonly string[];
}

// @public
export function resolveRelationship(rec: RelationshipRecord): ResolvedRelationship;

// @public
export type ResolveResult = {
    readonly ok: true;
    readonly contentType: string;
    readonly source: 'override' | 'default';
} | {
    readonly ok: false;
    readonly reason: 'unknown';
};

// @public
export function resolveTocRowHeadings(part: OoxmlPart, toc: DetectedToc, outline: readonly TocOutlineHeading[], excludeParagraphIds: ReadonlySet<string>): readonly (string | null)[];

// @public
export interface ResourceAccountingPort {
    // (undocumented)
    readonly kind: 'resource-accounting';
}

// @public
export interface ResourceLimits {
    readonly maxChunkBytes: number;
    readonly maxCompressedBytes: number;
    readonly maxCompressionRatio: number;
    readonly maxDecompressedBytes: number;
    readonly maxElementCount: number;
    readonly maxPaginationPasses: number;
    readonly maxPartCount: number;
    readonly maxQueueDepth: number;
    readonly maxRecursionDepth: number;
}

// @public
export const RESULT_IDS: {
    readonly applied: "dev.docx-editor.core.result.applied";
    readonly validation: "dev.docx-editor.core.result.validation";
    readonly conflict: "dev.docx-editor.core.result.conflict";
    readonly resource: "dev.docx-editor.core.result.resource";
    readonly authorization: "dev.docx-editor.core.result.authorization";
    readonly aborted: "dev.docx-editor.core.result.aborted";
};

// @public
export interface ReviewCommentItem {
    // (undocumented)
    readonly comment: CommentRecord;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly kind: 'comment';
    readonly orphaned: boolean;
    readonly parentId?: string;
    readonly parentRevisionId?: string;
    // (undocumented)
    readonly range: ReviewRange | null;
    readonly replyIds: readonly string[];
    // (undocumented)
    readonly resolved: boolean;
}

// @public
export type ReviewItem = ReviewRevisionItem | ReviewCommentItem;

// @public
export function reviewItemKey(item: ReviewItem): string;

// @public
export function reviewItemRanges(item: ReviewItem): readonly ReviewRange[];

// @public
export interface ReviewModelInput {
    readonly commentsExtendedPart?: OoxmlPart | undefined;
    readonly commentsPart?: OoxmlPart | undefined;
    readonly furnitureParts?: readonly OoxmlPart[] | undefined;
    readonly storyPart: OoxmlPart;
}

// @public
export interface ReviewPosition {
    // (undocumented)
    readonly offset: number;
    // (undocumented)
    readonly paragraphId: string;
}

// @public
export interface ReviewRange {
    // (undocumented)
    readonly end: ReviewPosition;
    // (undocumented)
    readonly partName: string;
    // (undocumented)
    readonly start: ReviewPosition;
}

// @public
export interface ReviewRevisionItem {
    readonly address: RevisionAddress;
    readonly addresses: readonly RevisionAddress[];
    // (undocumented)
    readonly author: string;
    // (undocumented)
    readonly date?: string;
    readonly id: string;
    // (undocumented)
    readonly kind: 'revision';
    readonly markDirection?: 'insert' | 'delete' | 'moveFrom' | 'moveTo';
    readonly nesting: number;
    readonly pairedWith?: string;
    readonly ranges: readonly ReviewRange[];
    readonly readOnly: boolean;
    readonly replacedRangeCount?: number;
    readonly replacedText: string;
    readonly replyIds: readonly string[];
    // (undocumented)
    readonly revisionKind: ReviewRevisionKind;
    readonly text: string;
}

// @public
export type ReviewRevisionKind = 'insert' | 'delete'
/**
* A deletion and an insertion that are one edit: text typed over a selection.
*
* Word shows these as a single `Replaced "x" with "y"` card, and resolving one half
* without the other is never what the reviewer meant — accepting the deletion alone
* leaves the replacement text unproposed, rejecting it alone leaves both.
*/
| 'replace' | 'moveFrom' | 'moveTo'
/** `w:rPrChange` / `w:pPrChange` — the words are unchanged, their formatting is not. */
| 'format'
/** `w:pPr/w:rPr/w:ins|w:del` — a paragraph split or merge. */
| 'paragraphMark'
/** A row, cell, section or grid revision. Supported row revisions are resolvable. */
| 'structural';

// @public
export interface RevisionAddress {
    // (undocumented)
    readonly author: string;
    readonly date?: string;
    // (undocumented)
    readonly id: string;
}

// @public
export function revisionItemsOf(part: OoxmlPart): readonly ReviewRevisionItem[];

// @public
export function runAddressRanges(paragraph: Extract<OoxmlNode, {
    kind: 'paragraph';
}>): Map<string, {
    start: number;
    end: number;
}>;

// @public
export interface RunPropertyEdit {
    // (undocumented)
    readonly end: number;
    // (undocumented)
    readonly properties: readonly OoxmlProperty[];
    // (undocumented)
    readonly start: number;
    readonly targetRunIds?: readonly string[];
}

// @public
export function runPropertyEdits(part: OoxmlPart, paragraphId: string, start: number, end: number, incoming: OoxmlProperty | readonly OoxmlProperty[]): readonly RunPropertyEdit[];

// @public
export function runsCovering(part: OoxmlPart, paragraphId: string, start: number, end: number): readonly OoxmlNode[];

// @public
export const RUNTIME_PORT_IDS: {
    readonly fonts: "dev.docx-editor.core.port.fonts";
    readonly shaping: "dev.docx-editor.core.port.shaping";
    readonly images: "dev.docx-editor.core.port.images";
    readonly clock: "dev.docx-editor.core.port.clock";
    readonly identity: "dev.docx-editor.core.port.identity";
    readonly persistence: "dev.docx-editor.core.port.persistence";
    readonly transport: "dev.docx-editor.core.port.transport";
    readonly scheduling: "dev.docx-editor.core.port.scheduling";
    readonly audit: "dev.docx-editor.core.port.audit";
    readonly authorization: "dev.docx-editor.core.port.authorization";
    readonly resourceAccounting: "dev.docx-editor.core.port.resource-accounting";
    readonly cancellation: "dev.docx-editor.core.port.cancellation";
    readonly externalResourceConsent: "dev.docx-editor.core.port.external-resource-consent";
};

// @public
export function sanitizeHref(raw: string): HrefProjection;

// @public
export function satisfies(version: string, range: string): boolean;

// @public
export interface SchedulingPort {
    // (undocumented)
    schedule(task: () => void): void;
}

// @public
export function scrubExport(items: readonly ContentItem[]): ScrubResult;

// @public
export interface ScrubResult {
    // (undocumented)
    readonly kept: readonly ContentItem[];
    readonly nonLossless: boolean;
    // (undocumented)
    readonly removed: readonly ContentItem[];
}

// @public
export const SEARCH_MATCH_LIMIT = 2000;

// @public
export const SEARCH_QUERY_MAX = 256;

// @public
export interface Segment {
    // (undocumented)
    readonly end: number;
    readonly formatRunIds?: readonly string[];
    // (undocumented)
    readonly node: OoxmlNode;
    readonly removeNodeIds?: readonly string[];
    // (undocumented)
    readonly runId: string;
    // (undocumented)
    readonly start: number;
}

// @public
export function segmentsOf(paragraph: OoxmlParagraphNode): Segment[];

// @public
export interface SelectionMark {
    // (undocumented)
    readonly end: number;
    // (undocumented)
    readonly paragraphId: string;
    // (undocumented)
    readonly start: number;
}

// @public
export interface SemanticDigest {
    // (undocumented)
    readonly stories: readonly StoryDigest[];
}

// @public
export function semanticDigest(parts: Iterable<OoxmlPart>): SemanticDigest;

// @public
export interface SemVer {
    // (undocumented)
    readonly major: number;
    // (undocumented)
    readonly minor: number;
    // (undocumented)
    readonly patch: number;
}

// @public
export class SequentialIdentity implements IdentityPort {
    constructor(prefix?: string);
    // (undocumented)
    newId(): string;
}

// @public
export function serializeOoxmlPart(part: OoxmlPart): string;

// @public
export function setCommentResolved(store: TreeDocumentStore, commentId: string, resolved: boolean): SetCommentResolvedResult;

// @public
export type SetCommentResolvedResult = {
    readonly ok: true;
    readonly changed: boolean;
    readonly change: TreeModelChange | null;
} | {
    readonly ok: false;
    readonly reason: TreeOpRejection | 'unknown-comment';
};

// @public
export function settingsPartOf(pkg: OoxmlPackage): OoxmlPart | null;

// @public
export interface ShapingPort {
    // (undocumented)
    readonly kind: 'shaping';
}

// @public
export function sniffImageMime(bytes: Uint8Array): RenderableImageMime | PreservedImageMime | 'unknown';

// @public
export interface SourceCrop {
    // (undocumented)
    readonly bottom: number;
    // (undocumented)
    readonly left: number;
    // (undocumented)
    readonly right: number;
    // (undocumented)
    readonly top: number;
}

// @public
export function stableHash(value: unknown, ephemera?: ReadonlySet<string>): string;

// @public
export interface StoryDigest {
    // (undocumented)
    readonly paragraphs: readonly ParagraphDigest[];
    // (undocumented)
    readonly partName: string;
    readonly structure: readonly string[];
}

// @public
export interface StoryIndexEntry {
    // (undocumented)
    readonly paragraphs: readonly ParagraphIndexEntry[];
    readonly partName: string;
    readonly rootId: string;
}

// @public
export function storyParagraphs(root: OoxmlNode): readonly OoxmlNode[];

// @public
export type StoryResolveResult = {
    readonly ok: true;
    readonly story: TreeStoryRef;
    readonly store: TreeDocumentStore;
} | {
    readonly ok: false;
    readonly reason: StoryTargetRejection;
    readonly detail?: string;
};

// @public
export function storyRootsOf(part: OoxmlPart): readonly OoxmlStoryRoot[];

// @public
export type StoryScope = {
    readonly kind: 'body';
} | {
    readonly kind: 'headerFooter';
    readonly rId: string;
} | {
    readonly kind: 'notesPart';
    readonly noteKind: NoteKind;
};

// @public
export type StoryTargetRejection = 'unknown-scope' | 'dangling-relationship' | 'wrong-relationship-type' | 'external-relationship' | 'bad-relationship-target' | 'missing-part' | 'not-a-story-part' | 'too-many-story-stores';

// @public
export interface StyleIndexEntry {
    // (undocumented)
    readonly basedOn: string | null;
    readonly isDefault: boolean;
    // (undocumented)
    readonly name: string | null;
    // (undocumented)
    readonly nodeId: string;
    // (undocumented)
    readonly styleId: string;
    // (undocumented)
    readonly type: string;
}

// @public
export function stylesPartOf(pkg: OoxmlPackage): OoxmlPart | undefined;

// @public
export type SupportedImageMime = 'image/png' | 'image/jpeg' | 'image/gif' | 'image/bmp' | 'image/webp';

// @public
export function sweepCustomNodePayloads(pkg: OoxmlPackage, storyPartName: string, namespaces: readonly string[]): CustomNodeSweepResult;

// @public
export const TABLE_BORDER_STYLES: readonly ["single", "dashed", "dotted", "double", "triple", "thick"];

// @public
export type TableBorderStyle = (typeof TABLE_BORDER_STYLES)[number];

// @public
export type TargetMode = 'Internal' | 'External';

// @public
export function textContent(node: Extract<XmlNode, {
    type: 'element';
}>): string;

// @public
export interface TextMatchOptions {
    readonly from?: number;
    // (undocumented)
    readonly matchCase?: boolean;
    // (undocumented)
    readonly to?: number;
    // (undocumented)
    readonly wholeWord?: boolean;
}

// @public
export interface TextOccurrence {
    // (undocumented)
    readonly length: number;
    // (undocumented)
    readonly start: number;
}

// @public
export interface TextOccurrences {
    // (undocumented)
    readonly matches: readonly TextOccurrence[];
    readonly truncated: boolean;
}

// @public
export function threadStateOfPart(part: OoxmlPart): Map<string, CommentThreadState>;

// @public
export const TOC_LEVEL_INDENT_TWIPS = 240;

// @public
export const TOC_MAX_BOOKMARKS_PER_REFRESH = 512;

// @public
export const TOC_MAX_ENTRIES = 512;

// @public
export const TOC_MAX_FIELD_NESTING = 4;

// @public
export const TOC_MAX_INSTRUCTION_CHARS = 256;

// @public
export const TOC_MAX_PAGE_PASSES = 3;

// @public
export interface TocEntryPlan {
    // (undocumented)
    readonly bookmarkName: string;
    // (undocumented)
    readonly headingParagraphId: string;
    // (undocumented)
    readonly level: number;
    // (undocumented)
    readonly pageNumberText: string;
    // (undocumented)
    readonly text: string;
}

// @public
export function tocEntryText(text: string): string;

// @public
export interface TocInstruction {
    readonly hyperlink: boolean;
    // (undocumented)
    readonly keyword: 'TOC';
    readonly omitPageNumbers: boolean;
    // (undocumented)
    readonly outlineEnd: number;
    readonly outlineStart: number;
    // (undocumented)
    readonly raw: string;
}

// @public
export function tocLeftIndentTwips(level: number): number;

// @public
export interface TocOutlineHeading {
    // (undocumented)
    readonly blockId: string;
    // (undocumented)
    readonly level: number;
    // (undocumented)
    readonly text: string;
}

// @public
export function toSafeRecord(value: unknown, path?: string): unknown;

// @public
export interface TransportPort {
    // (undocumented)
    readonly kind: 'transport';
}

// @public
export const TREE_DOC_OP_KINDS: readonly ["insertText", "deleteText", "setParagraphMarkRevision", "proposeParagraphMerge", "insertCommentMarker", "acceptRevision", "rejectRevision", "acceptAllRevisions", "rejectAllRevisions", "insertTab", "insertHardBreak", "insertPageBreak", "insertPageField", "setListLevel", "setListNumbering", "setParagraphMarkProperties", "splitParagraph", "splitParagraphMany", "joinParagraphs", "setRunProperties", "setParagraphProperties", "setSectionProperties", "setSectionMark", "insertHyperlink", "setHyperlinkTarget", "removeHyperlink", "setContentControlValue", "removeContentControl", "insertInlineContentControl", "addRepeatingSectionItem", "removeRepeatingSectionItem", "deleteBlock", "insertTable", "insertTableRow", "deleteTableRow", "insertTableColumn", "deleteTableColumn", "setTableColumnWidths", "setTableRightEdgeWidth", "setTableRowHeight", "setTableCellBorders", "setTableCellFill", "setTableCellVerticalAlignment", "createHeaderFooter", "deleteHeaderFooter", "linkToPrevious", "unlinkFromPrevious", "setSectionFurnitureOptions", "insertNote", "deleteNote", "convertNote", "convertAllNotes", "setNoteProperties", "setContentControlProperties", "insertContentControl", "insertDrawing", "replaceDrawingResource", "deleteDrawing", "resizeDrawing", "cropDrawing", "positionDrawing", "setDrawingWrap", "setDrawingMetadata", "setDrawingLocks", "transformDrawing", "insertToc", "replaceTocResult", "rewriteTocPageNumbers"];

// @public
export type TreeDocOp = {
    readonly op: 'insertToc';
    readonly beforeParagraphId: string;
    readonly instruction: string;
    readonly alias: string;
    readonly entries: readonly {
        readonly level: number;
        readonly text: string;
        readonly headingParagraphId: string;
        readonly bookmarkName: string;
        readonly pageNumberText: string;
    }[];
    readonly bookmarksToCreate: readonly {
        readonly paragraphId: string;
        readonly name: string;
    }[];
} | {
    readonly op: 'insertText';
    readonly paragraphId: string;
    readonly offset: number;
    readonly text: string;
    readonly revision?: RevisionAttributionInput;
    readonly inside?: string;
    readonly bias?: 'left' | 'right';
} | {
    readonly op: 'deleteText';
    readonly paragraphId: string;
    readonly start: number;
    readonly end: number;
    readonly revision?: RevisionAttributionInput;
} | {
    readonly op: 'setParagraphMarkRevision';
    readonly paragraphId: string;
    readonly kind: 'ins' | 'del';
    readonly revision: RevisionAttributionInput;
} | {
    readonly op: 'proposeParagraphMerge';
    readonly paragraphId: string;
    readonly revision: RevisionAttributionInput;
} | {
    readonly op: 'insertCommentMarker';
    readonly paragraphId: string;
    readonly offset: number;
    readonly commentId: string;
    readonly marker: 'start' | 'end' | 'reference';
} | {
    readonly op: 'acceptRevision';
    readonly revision: RevisionAddress;
    readonly localName?: string;
} | {
    readonly op: 'rejectRevision';
    readonly revision: RevisionAddress;
    readonly localName?: string;
} | {
    readonly op: 'acceptAllRevisions';
    readonly scopeRootId?: string;
} | {
    readonly op: 'rejectAllRevisions';
    readonly scopeRootId?: string;
} | {
    readonly op: 'insertTab';
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'insertHardBreak';
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'insertPageBreak';
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'insertPageField';
    readonly paragraphId: string;
    readonly offset: number;
    readonly field: 'PAGE' | 'NUMPAGES' | 'SECTIONPAGES' | 'PAGE_X_OF_Y';
} | {
    readonly op: 'setListLevel';
    readonly paragraphId: string;
    readonly level: number;
} | {
    readonly op: 'setParagraphMarkProperties';
    readonly paragraphId: string;
    readonly properties: readonly OoxmlProperty[];
} | {
    readonly op: 'setListNumbering';
    readonly paragraphId: string;
    readonly numId: string | null;
    readonly level?: number;
} | {
    readonly op: 'splitParagraph';
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'splitParagraphMany';
    readonly paragraphId: string;
    readonly offsets: readonly number[];
} | {
    readonly op: 'joinParagraphs';
    readonly firstId: string;
    readonly secondId: string;
} | {
    readonly op: 'setRunProperties';
    readonly paragraphId: string;
    readonly start: number;
    readonly end: number;
    readonly properties: readonly OoxmlProperty[];
    readonly targetRunIds?: readonly string[];
} | {
    readonly op: 'setParagraphProperties';
    readonly paragraphId: string;
    readonly properties: readonly OoxmlProperty[];
} | {
    readonly op: 'setSectionProperties';
    readonly pageWidthTwips?: number;
    readonly pageHeightTwips?: number;
    readonly orientation?: 'portrait' | 'landscape';
    readonly marginTopTwips?: number;
    readonly marginRightTwips?: number;
    readonly marginBottomTwips?: number;
    readonly marginLeftTwips?: number;
    readonly anchorParagraphId?: string;
} | {
    readonly op: 'setSectionMark';
    readonly paragraphId: string;
} | {
    readonly op: 'insertHyperlink';
    readonly paragraphId: string;
    readonly start: number;
    readonly end: number;
    readonly relationshipId?: string;
    readonly anchor?: string;
    readonly tooltip?: string;
    readonly styleId?: string;
} | {
    readonly op: 'setHyperlinkTarget';
    readonly linkId: string;
    readonly relationshipId?: string;
    readonly anchor?: string;
    readonly tooltip?: string;
} | {
    readonly op: 'removeHyperlink';
    readonly linkId: string;
} | {
    readonly op: 'insertInlineContentControl';
    readonly paragraphId: string;
    readonly offset: number;
    readonly tag: string;
    readonly text: string;
    readonly alias?: string;
    readonly lock?: 'sdtLocked' | 'sdtContentLocked' | 'contentLocked';
    readonly dataBinding?: {
        readonly prefixMappings: string;
        readonly xpath: string;
        readonly storeItemId: string;
    };
} | {
    readonly op: 'addRepeatingSectionItem';
    readonly controlId: string;
    readonly index?: number;
} | {
    readonly op: 'removeRepeatingSectionItem';
    readonly controlId: string;
    readonly index: number;
} | {
    readonly op: 'deleteBlock';
    readonly blockId: string;
} | {
    readonly op: 'insertTable';
    readonly beforeParagraphId: string;
    readonly rows: number;
    readonly cols: number;
    readonly columnWidthTwips: number;
    readonly cellText?: readonly (readonly string[])[];
} | {
    readonly op: 'insertTableRow';
    readonly tableId: string;
    readonly rowId: string;
    readonly where: 'above' | 'below';
    readonly revision?: RevisionAttributionInput;
} | {
    readonly op: 'deleteTableRow';
    readonly tableId: string;
    readonly rowId: string;
    readonly referenceCellId?: string;
    readonly revision?: RevisionAttributionInput;
} | {
    readonly op: 'insertTableColumn';
    readonly tableId: string;
    readonly where: 'left' | 'right';
    readonly gridColumnId: string;
} | {
    readonly op: 'insertTableColumn';
    readonly tableId: string;
    readonly where: 'left' | 'right';
    readonly referenceCellId: string;
} | {
    readonly op: 'deleteTableColumn';
    readonly tableId: string;
    readonly gridColumnId: string;
} | {
    readonly op: 'setTableColumnWidths';
    readonly tableId: string;
    readonly leftGridColumnId: string;
    readonly rightGridColumnId: string;
    readonly leftWidthTwips: number;
    readonly rightWidthTwips: number;
} | {
    readonly op: 'setTableRightEdgeWidth';
    readonly tableId: string;
    readonly gridColumnId: string;
    readonly columnWidthTwips: number;
    readonly tableWidthTwips: number;
} | {
    readonly op: 'setTableRowHeight';
    readonly tableId: string;
    readonly rowId: string;
    readonly heightTwips: number;
} | {
    readonly op: 'setTableCellBorders';
    readonly tableId: string;
    readonly cellIds: readonly string[];
    readonly scope: 'none';
    readonly target: TableBorderEdgeTarget;
} | {
    readonly op: 'setTableCellBorders';
    readonly tableId: string;
    readonly cellIds: readonly string[];
    readonly scope: TableBorderEdgeTarget;
    readonly spec: TableBorderSpecInput;
} | {
    readonly op: 'setTableCellFill';
    readonly tableId: string;
    readonly cellIds: readonly string[];
    readonly color: TreeDocColorValue | null;
} | {
    readonly op: 'setTableCellVerticalAlignment';
    readonly tableId: string;
    readonly cellIds: readonly string[];
    readonly alignment: 'top' | 'center' | 'bottom';
} | {
    readonly op: 'createHeaderFooter';
    readonly sectionIndex: number;
    readonly kind: 'header' | 'footer';
    readonly variant: 'default' | 'first' | 'even';
    readonly titlePage?: boolean;
    readonly evenAndOddHeaders?: boolean;
} | {
    readonly op: 'deleteHeaderFooter';
    readonly sectionIndex: number;
    readonly kind: 'header' | 'footer';
    readonly variant: 'default' | 'first' | 'even';
} | {
    readonly op: 'linkToPrevious';
    readonly sectionIndex: number;
    readonly kind: 'header' | 'footer';
    readonly variant: 'default' | 'first' | 'even';
} | {
    readonly op: 'unlinkFromPrevious';
    readonly sectionIndex: number;
    readonly kind: 'header' | 'footer';
    readonly variant: 'default' | 'first' | 'even';
} | {
    readonly op: 'setSectionFurnitureOptions';
    readonly sectionIndex?: number;
    readonly titlePage?: boolean;
    readonly evenAndOddHeaders?: boolean;
    readonly headerDistanceTwips?: number;
    readonly footerDistanceTwips?: number;
} | {
    readonly op: 'insertNote';
    readonly noteKind: 'footnote' | 'endnote';
    readonly paragraphId: string;
    readonly offset: number;
} | {
    readonly op: 'deleteNote';
    readonly noteKind: 'footnote' | 'endnote';
    readonly noteId: number;
} | {
    readonly op: 'convertNote';
    readonly fromKind: 'footnote' | 'endnote';
    readonly noteId: number;
} | {
    readonly op: 'convertAllNotes';
    readonly fromKind: 'footnote' | 'endnote';
} | {
    readonly op: 'setContentControlValue';
    readonly controlId: string;
    readonly value: string | ContentControlValueInput;
} | {
    readonly op: 'setContentControlProperties';
    readonly controlId: string;
    readonly tag?: string | null;
    readonly alias?: string | null;
    readonly lock?: ContentControlLock;
} | {
    readonly op: 'removeContentControl';
    readonly controlId: string;
    readonly keepContent?: boolean;
} | {
    readonly op: 'insertContentControl';
    readonly paragraphId: string;
    readonly start: number;
    readonly end: number;
    readonly type: InsertableContentControlType;
    readonly tag?: string;
    readonly alias?: string;
    readonly lock?: ContentControlLock;
} | {
    readonly op: 'setNoteProperties';
    readonly scope: 'document' | 'section';
    readonly sectionIndex?: number;
    readonly footnote?: {
        readonly numFmt?: string;
        readonly numRestart?: string;
        readonly position?: string;
        readonly numStart?: number;
    };
    readonly endnote?: {
        readonly numFmt?: string;
        readonly numRestart?: string;
        readonly position?: string;
        readonly numStart?: number;
    };
} | {
    readonly op: 'insertDrawing';
    readonly paragraphId: string;
    readonly offset: number;
    readonly drawing: OoxmlDrawingNode;
} | {
    readonly op: 'replaceDrawingResource';
    readonly drawingNodeId: string;
    readonly relationshipId: string;
} | {
    readonly op: 'deleteDrawing';
    readonly drawingNodeId: string;
    readonly revision?: RevisionAttributionInput;
} | {
    readonly op: 'resizeDrawing';
    readonly drawingNodeId: string;
    readonly extentEmu: {
        readonly cx: number;
        readonly cy: number;
    };
} | {
    readonly op: 'cropDrawing';
    readonly drawingNodeId: string;
    readonly crop: SourceCrop;
} | {
    readonly op: 'positionDrawing';
    readonly drawingNodeId: string;
    readonly position: DrawingPositionInput;
} | {
    readonly op: 'setDrawingWrap';
    readonly drawingNodeId: string;
    readonly wrap: ImageWrapTarget;
} | {
    readonly op: 'setDrawingMetadata';
    readonly drawingNodeId: string;
    readonly title: string;
    readonly description: string;
    readonly hyperlink?: string | null;
} | {
    readonly op: 'setDrawingLocks';
    readonly drawingNodeId: string;
    readonly locks: DrawingLocksInput;
} | {
    readonly op: 'transformDrawing';
    readonly drawingNodeId: string;
    readonly action: 'rotateCW' | 'rotateCCW' | 'flipH' | 'flipV';
} | {
    readonly op: 'replaceTocResult';
    readonly tocId: string;
    readonly entries: readonly {
        readonly level: number;
        readonly text: string;
        readonly headingParagraphId: string;
        readonly bookmarkName: string;
        readonly pageNumberText: string;
    }[];
    readonly bookmarksToCreate: readonly {
        readonly paragraphId: string;
        readonly name: string;
    }[];
} | {
    readonly op: 'rewriteTocPageNumbers';
    readonly tocId: string;
    readonly updates: readonly {
        readonly paragraphId: string;
        readonly pageNumberText: string;
    }[];
};

// @public
export type TreeDocOpKind = TreeDocOp['op'];

// @public
export class TreeDocumentStore {
    constructor(source: OoxmlPart | OoxmlPackage, storyPartNameOrOptions?: string | TreeDocumentStoreOptions, maybeOptions?: TreeDocumentStoreOptions);
    beginComposition(selectionBefore?: SelectionMark | null): void;
    cancelComposition(): void;
    // (undocumented)
    get canRedo(): boolean;
    // (undocumented)
    get canUndo(): boolean;
    checkpoint(): TreeDocumentCheckpoint;
    // (undocumented)
    get compositionActive(): boolean;
    endComposition(): void;
    graftPackage(edit: (pkg: OoxmlPackage) => OoxmlPackage): void;
    get historyDepth(): number;
    get package(): OoxmlPackage;
    get part(): OoxmlPart;
    // (undocumented)
    redo(): TreeModelChange | null;
    replacePart(part: OoxmlPart): void;
    restoreCheckpoint(checkpoint: TreeDocumentCheckpoint): void;
    restoreHistoryStacks(checkpoint: TreeDocumentCheckpoint): void;
    // (undocumented)
    get revision(): number;
    selectionForRedo(): SelectionMark | null;
    selectionForUndo(): SelectionMark | null;
    setStoryRef(story: TreeStoryRef | null): void;
    // (undocumented)
    subscribe(listener: (change: TreeModelChange) => void): () => void;
    transact(build: (ctx: TreeTransactionContext) => void, options?: TreeTransactOptions): TreeTransactResult;
    // (undocumented)
    undo(): TreeModelChange | null;
}

// @public
export interface TreeDocumentStoreOptions {
    readonly historyLimit?: number;
}

// @public
export interface TreeModelChange {
    readonly caret?: SelectionMark;
    // (undocumented)
    readonly change: 'model-change';
    // (undocumented)
    readonly commitId: string;
    // (undocumented)
    readonly created: readonly string[];
    // (undocumented)
    readonly deleted: readonly string[];
    // (undocumented)
    readonly dependencyKeys: readonly string[];
    // (undocumented)
    readonly dirty: readonly string[];
    // (undocumented)
    readonly fromRevision: number;
    readonly impact: ImpactClass;
    // (undocumented)
    readonly origin: string;
    // (undocumented)
    readonly splitJoin: readonly ({
        readonly split: {
            readonly from: string;
            readonly tail: string;
        };
    } | {
        readonly join: {
            readonly kept: string;
            readonly removed: string;
        };
    })[];
    readonly story?: TreeStoryRef;
    // (undocumented)
    readonly toRevision: number;
}

// @public
export interface TreeOpEffect {
    readonly caret?: {
        readonly paragraphId: string;
    };
    // (undocumented)
    readonly created: readonly string[];
    // (undocumented)
    readonly deleted: readonly string[];
    // (undocumented)
    readonly dependencyKeys: readonly string[];
    // (undocumented)
    readonly dirty: readonly string[];
    // (undocumented)
    readonly impact: ImpactClass;
    // (undocumented)
    readonly join?: {
        readonly kept: string;
        readonly removed: string;
    };
    // (undocumented)
    readonly split?: {
        readonly from: string;
        readonly tail: string;
    };
    readonly splits?: readonly {
        readonly from: string;
        readonly tail: string;
    }[];
}

// @public
export type TreeOpRejection = 'unknown-op' | 'unknown-paragraph' | 'not-a-paragraph' | 'offset-out-of-range' | 'invalid-range' | 'not-a-list-paragraph' | 'splits-surrogate-pair' | 'invalid-text' | 'unsupported-property' | 'invalid-property-value' | 'not-adjacent-siblings' | 'unknown-block' | 'not-a-block' | 'block-required' | 'carries-section-mark'
/** The transaction named a part the package does not hold. */
| 'unknown-part'
/**
* The transaction would have published a package that does not open: a relationship
* pointing at a part nobody created, or a part with no declared content type.
*/
| 'package-invariant'
/** No revision in this part carries the addressed `(id, author, date)` triple. */
| 'unknown-revision'
/**
* A matched revision is a kind whose accept/reject semantics are structural and not
* implemented. Refusing is deliberate: removing the markup alone would report the decision
* applied while leaving the row, cell, or section it describes untouched.
*/
| 'unsupported-revision' | 'tree-invariant'
/** No content control in this part carries the addressed node id. */
| 'unknown-content-control'
/** The addressed node exists and is not a `w:sdt`. */
| 'not-a-content-control'
/**
* A content control's `w:lock` — or one an enclosing control imposes — forbids this.
*
* The same code for an edit inside `contentLocked` content and for the removal of an
* `sdtLocked` control: both are "the document says no", and the two halves are already
* distinguished by which operation was refused.
*/
| 'locked'
/** The control declares `w:dataBinding`; its value belongs to a custom XML part. */
| 'bound'
/** The value offered is not one this control's type accepts. */
| 'typeMismatch' | 'unknown-control' | 'unsupported'
/** Malformed lifecycle args / first-section link — mirrors Editor `invalidArgs`. */
| 'invalidArgs'
/** The addressed table id is missing, duplicated, or not a typed table. */
| 'unknown-table'
/** The addressed row id is missing or not a direct child of the table. */
| 'unknown-row'
/** A table property container appears more than once on a typed node. */
| 'duplicate-property-container'
/** Row insertion would split an active vertical-merge chain. */
| 'vertical-merge-crossing'
/** Column edit refused because the table carries horizontal or vertical merges. */
| 'table-has-merge'
/** The addressed grid column id is missing or ambiguous without `w:tblGrid`. */
| 'unknown-grid-column'
/** The operation would exceed bounded table topology limits. */
| 'resource-limit'
/** The addressed node is not a top-level `w:drawing`. */
| 'not-a-drawing'
/** No `w:drawing` with this id exists in the part. */
| 'unknown-drawing'
/** The drawing's graphic payload is not a supported picture. */
| 'not-a-picture-drawing'
/** A lock flag or `@locked` forbids this mutation. */
| 'drawing-locked'
/** Finite EMU extent, crop, or position value is out of range. */
| 'invalid-drawing-value'
/** Insertion would cross a table-cell boundary or wrong story container. */
| 'cross-cell-drawing'
/** Suggesting-mode drawing deletion is not implemented in this change. */
| 'trackedDrawingDeletionUnsupported'
/** Hyperlink target creation or change needs an OPC relationship in a package transaction. */
| 'packageTransactionRequired';

// @public
export type TreeOpResult = {
    readonly ok: true;
    readonly part: OoxmlPart;
    readonly effect: TreeOpEffect;
} | {
    readonly ok: false;
    readonly reason: TreeOpRejection;
    readonly detail?: string;
};

// @public
export class TreePackageStore {
    constructor(pkg: OoxmlPackage, main: OoxmlPart, options?: TreePackageStoreOptions);
    adoptPackageUnit(before: OoxmlPackage): void;
    applyImageProperties(scope: StoryScope, input: ApplyImagePropertiesInput): ImageIntentResult;
    applyLifecycleOp(op: HeaderFooterLifecycleOp | NoteLifecycleOp | TreeDocOp): PackageTransactResult;
    // (undocumented)
    beginComposition(scope: StoryScope, selectionBefore?: SelectionMark | null): boolean;
    bodyStore(): TreeDocumentStore;
    // (undocumented)
    cancelComposition(): void;
    // (undocumented)
    get canRedo(): boolean;
    // (undocumented)
    get canUndo(): boolean;
    compositionSessionOpen(): boolean;
    currentPackage(): OoxmlPackage;
    deleteImage(scope: StoryScope, drawingNodeId: string): ImageIntentResult;
    embedExternalImage(scope: StoryScope, drawingNodeId: string, url: string, port: ExternalImageFetchPort, signal: AbortSignal, decodePort: ImageDecodePort): Promise<ImageIntentResult>;
    // (undocumented)
    endComposition(): void;
    insertImage(scope: StoryScope, input: InsertImageInput): Promise<ImageIntentResult>;
    installPackageSnapshot(snapshot: OoxmlPackage): void;
    // (undocumented)
    get lastModelChange(): TreeModelChange | null;
    openedStoryCount(): number;
    // (undocumented)
    get packageRevision(): number;
    partFor(scope: StoryScope): OoxmlPart | null;
    promoteStoryTransactionToPackageUnit(beforePackage: OoxmlPackage, store: TreeDocumentStore, checkpoint: TreeDocumentCheckpoint, beforeDepth: number): TreeModelChange;
    publishStoryWrite(change: TreeModelChange | null): TreeModelChange | null;
    // (undocumented)
    redo(): TreeModelChange | null;
    replaceImage(scope: StoryScope, drawingNodeId: string, bytes: Uint8Array, mime: SupportedImageMime, decodePort: ImageDecodePort, options: ReplaceImageOptions): Promise<ImageIntentResult>;
    replacePackageShell(pkg: OoxmlPackage): void;
    resolveStory(scope: StoryScope): StoryResolveResult;
    revisionFor(scope: StoryScope): number | null;
    // (undocumented)
    selectionForRedo(): SelectionMark | null;
    // (undocumented)
    selectionForUndo(): SelectionMark | null;
    setDrawingMetadataWithHyperlink(scope: StoryScope, drawingNodeId: string, title: string, description: string, hyperlink: string | null): ImageIntentResult;
    // (undocumented)
    subscribe(listener: (change: TreeModelChange) => void): () => void;
    transact(scope: StoryScope, build: (ctx: TreeTransactionContext) => void, options?: Omit<TreeTransactOptions, 'story' | 'minimumImpact'>): PackageTransactResult;
    // (undocumented)
    undo(): TreeModelChange | null;
}

// @public
export interface TreePackageStoreOptions {
    readonly cascadeDeletedNoteReferences?: NoteCascadeFn;
    // (undocumented)
    readonly historyLimit?: number;
    readonly maxEditableStoryParts?: number;
}

// @public
export type TreeStoryRef = {
    readonly kind: 'body';
    readonly partName: string;
} | {
    readonly kind: 'headerFooter';
    readonly partName: string;
    readonly rId: string;
} | {
    readonly kind: 'notesPart';
    readonly partName: string;
    readonly noteKind: 'footnote' | 'endnote';
};

// @public
export interface TreeTransactionContext {
    apply(op: TreeDocOp): boolean;
    applyPackage(edit: (pkg: OoxmlPackage) => OoxmlPackage): boolean;
    applyTo(partName: string, op: TreeDocOp): boolean;
    selectionAfter(selection: SelectionMark | null): void;
    selectionBefore(selection: SelectionMark | null): void;
}

// @public
export interface TreeTransactOptions {
    readonly minimumImpact?: ImpactClass;
    // (undocumented)
    readonly origin?: string;
    readonly scope?: 'transaction' | 'command';
    readonly story?: TreeStoryRef;
}

// @public
export type TreeTransactResult = {
    readonly ok: true;
    readonly change: TreeModelChange | null;
} | {
    readonly ok: false;
    readonly reason: TreeOpRejection;
    readonly detail?: string;
};

// @public
export function usedParaIds(root: OoxmlElement): ReadonlySet<string>;

// @public
export interface ValidatedRasterHeader {
    // (undocumented)
    readonly pixelHeight: number;
    // (undocumented)
    readonly pixelWidth: number;
}

// @public
export function validateDrawingOp(part: OoxmlPart, op: DrawingTreeDocOp): TreeOpRejection | null;

// @public
export function validateExternalTarget(raw: string): NameResult;

// @public
export function validateFixture(fixture: ConformanceFixture): ValidationResult;

// @public
export function validateGifHeader(bytes: Uint8Array): ValidatedRasterHeader | null;

// @public
export function validateJpegHeader(bytes: Uint8Array): ValidatedRasterHeader | null;

// @public
export function validateOoxmlPart(part: OoxmlPart): OoxmlInvariantResult;

// @public
export function validatePackageInvariants(pkg: OoxmlPackage): PackageInvariantResult;

// @public
export function validatePngHeader(bytes: Uint8Array): ValidatedRasterHeader | null;

// @public
export function validateRasterHeader(bytes: Uint8Array, mime: SupportedImageMime): ValidatedRasterHeader | null;

// @public
export function validateTreeOp(part: OoxmlPart, op: TreeDocOp): TreeOpRejection | null;

// @public
export interface ValidationResult {
    // (undocumented)
    readonly errors: readonly string[];
    // (undocumented)
    readonly valid: boolean;
}

// @public
export function w14RootPrefix(root: OoxmlElement): string | null;

// @public
export const W15_NAMESPACE_URI = "http://schemas.microsoft.com/office/word/2012/wordml";

// @public
export function walkAllStoryParagraphs(children: readonly OoxmlNode[], sdtDepth: number, visit: (paragraph: OoxmlElement) => void): void;

// @public
export function walkParagraphInline(children: readonly OoxmlNode[], depth: number, visit: (child: OoxmlNode) => void): void;

// @public
export function walkStoryBlocks(children: readonly OoxmlNode[], depth: number, visit: (block: OoxmlElement) => void): void;

// @public
export function withBinaryPart(pkg: OoxmlPackage, partName: string, bytes: Uint8Array, contentType: string): OoxmlPackage;

// @public
export function withContentTypeOverride(pkg: OoxmlPackage, partName: string, contentType: string, options?: {
    readonly forceOverride?: boolean;
}): OoxmlPackage;

// @public
export function withCustomXmlDataPart(pkg: OoxmlPackage, storyPartName: string, namespaceUri: string, rootLocalName: string): CustomXmlDataPartResult;

// @public
export function withCustomXmlNode(pkg: OoxmlPackage, partName: string, node: CustomXmlNode): OoxmlPackage;

// @public
export function withEmbeddedImage(pkg: OoxmlPackage, ownerPartName: string, input: Readonly<{
    bytes: Uint8Array;
    mime: SupportedImageMime;
}>): Readonly<{
    ok: true;
    pkg: OoxmlPackage;
    partName: string;
    relationshipId: string;
    docPrId: number;
}> | Readonly<{
    ok: false;
    reason: 'invalidArgs' | 'invalid-image';
}>;

// @public
export function withExportedCustomNodes(pkg: OoxmlPackage, request: CustomNodeExportRequest): CustomNodeExportResult;

// @public
export function withNewPart(pkg: OoxmlPackage, partName: string, root: OoxmlElement, contentType: string): OoxmlPackage;

// @public
export function withoutCustomXmlDataPart(pkg: OoxmlPackage, storyPartName: string, namespaceUri: string): WithoutPartResult;

// @public
export function withoutCustomXmlNode(pkg: OoxmlPackage, partName: string, nodeId: string): OoxmlPackage;

// @public
export function withoutOrphanCustomXmlNodes(pkg: OoxmlPackage, partName: string, referencedIds: ReadonlySet<string>): {
    readonly pkg: OoxmlPackage;
    readonly removed: readonly string[];
};

// @public
export function withoutUnreferencedImagePart(pkg: OoxmlPackage, partName: string): OoxmlPackage;

// @public
export function withPart(pkg: OoxmlPackage, part: OoxmlPart): OoxmlPackage;

// @public
export function withRelationship(pkg: OoxmlPackage, ownerPart: string, type: string, rawTarget: string): {
    readonly pkg: OoxmlPackage;
    readonly relationshipId: string;
    readonly ok: boolean;
};

// @public
export const WML_NAMESPACE_URI = "http://schemas.openxmlformats.org/wordprocessingml/2006/main";

// @public
export function wrapTargetToAnchorSpec(target: ImageWrapTarget): {
    readonly behindDocument: boolean;
    readonly wrapLocalName: 'wrapNone' | 'wrapSquare' | 'wrapTight' | 'wrapThrough' | 'wrapTopAndBottom';
    readonly wrapText?: 'bothSides' | 'left' | 'right' | 'largest';
};

// @public
export function writeOoxmlPackage(pkg: OoxmlPackage): Uint8Array;

// @public
export function writeZip(entries: ReadonlyMap<string, Uint8Array>): Uint8Array;

// @public
export const XML_NAMESPACE_URI = "http://www.w3.org/XML/1998/namespace";

// @public
export interface XmlLimits {
    // (undocumented)
    readonly maxBytes: number;
    // (undocumented)
    readonly maxElements?: number;
}

// @public
export type XmlNode = {
    readonly type: 'element';
    readonly name: string;
    readonly attributes: Readonly<Record<string, string>>;
    readonly children: readonly XmlNode[];
} | {
    readonly type: 'text';
    readonly value: string;
};

// @public
export const XMLNS_NAMESPACE_URI = "http://www.w3.org/2000/xmlns/";

// @public
export type XmlRejection = 'too-large' | 'dtd-forbidden' | 'entity-forbidden' | 'too-deep' | 'too-many-elements' | 'invalid-limits' | 'parse-error';

// @public
export type XmlResult = {
    readonly ok: true;
    readonly nodes: readonly XmlNode[];
} | {
    readonly ok: false;
    readonly reason: XmlRejection;
};

// @public
export interface ZipLimits {
    // (undocumented)
    readonly maxEntries: number;
    readonly maxRatio?: number;
    readonly maxTotalBytes: number;
}

// @public
export type ZipReadResult = {
    readonly ok: true;
    readonly entries: ReadonlyMap<string, Uint8Array>;
} | {
    readonly ok: false;
    readonly reason: ZipRejection;
    readonly detail?: string;
};

// @public
export type ZipRejection = 'too-many-entries' | 'too-large' | 'bad-name' | 'inflate-error';

// (No @packageDocumentation comment for this package)

```
